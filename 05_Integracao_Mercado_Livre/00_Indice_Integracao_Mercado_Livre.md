---
tipo: regra
dominio: 
status: ativa
criado: 06/08/2026
atualizado_em: 30/08/2026 13:33
relacionado: [Padrao de Robustez para Clientes de API Externa, Regra do Índice Obrigatório]
---

# Índice — Integração Mercado Livre

Índice obrigatório deste mundo — 1 linha de resumo por nota, agrupado por contexto/área. Atualizado junto da autorização de escrita de cada nota (ver [[Regra do Índice Obrigatório]]).

Mundo criado em 06/08/2026, mesma lógica do `03_Integracao_Sysemp/`: API grande o suficiente pra justificar ser testada e documentada de forma isolada. Existe um projeto anterior e diferente (`03_ML_Analytics_HUB/`, congelado em `LEGADO/`) que serve de fonte de lições aprendidas sobre como blindar chamadas a essa mesma API, não de continuidade direta.

**Integração começou de fato em 12/08/2026** — código já existente e em uso numa pasta separada do computador, migrado pro repo `Projeto_Sistema_Interno_V2` por etapas, começando pela base de autenticação + cliente HTTP.

O padrão de segurança/estrutura de cliente de API (throttle, backoff, hierarquia de exceção, nome de pacote) é cross-cutting e mora em [[Padrao de Robustez para Clientes de API Externa]], dentro de `02_Sistema_Interno/Regras_de_Comportamento/` — não duplicado aqui.

## Cliente_HTTP

| Nota | Tipo | Status | Data | Resumo |
|---|---|---|---|---|
| [[Migracao da API do Mercado Livre com Suporte a Multiplas Contas (MB e SV)]] | decisao | ativa | 12/08/2026 | Migração dos 3 arquivos base (auth OAuth2+PKCE, gerenciador de token, cliente HTTP) de pasta separada do computador pra `api_mercado_livre/core/`, padrão `api_<nome>/core`. Achado real: `.env` já dividido por conta (MB/SV) sem variável compartilhada — `gerenciador_token.py` corrigido pra exigir `conta` explícito, sem padrão, com lock de renovação por conta. Validado com chamada real (`GET /users/me`, HTTP 200) nas 2 contas, incluindo renovação automática de token. **Reconfirmado em 26/08, 09h17** — início do foco 100% em Integração ML: usuário revalidou ao vivo, HTTP 200 nas 2 contas de novo. Achado nesta revalidação: falta um Facade pro ML (nos moldes do `ApiSysemp(empresa=None)`, que já existe e já resolve `obter_empresa_ativa()` sozinho) — hoje a escolha MB/SV pro lado do ML ainda é manual, passada por quem chama. |
| [[Tratamento Detalhado e Relatorio Estruturado de Erros de Chamada a API do Mercado Livre]] | regra | ativa | 26/08/2026 | Nascida do estudo da camada superior da API. Regra 1: extrair todo campo de erro do ML que existir (`status` sempre; `error`/`message`/`cause`/`code`/`error_description`/`blocked_by` opcionais — lista ampliada 4x ao longo do estudo, formato comprovadamente não-fixo), hoje tudo ignorado por `cliente_api.py`. Regra 2: gerar 2 tabelas ao final de toda execução (ocorrência: Endpoint\|Erro\|Parâmetro; resumo: Endpoint\|Erro\|Quantidade). Escopo confirmado: só Mercado Livre, não cross-cutting com Sysemp. Decisão relacionada: **não** construir espaçador proativo de chamadas por enquanto (3 motivos — custo fixo escala mal, uso não é contínuo, webhook deve reduzir volume no futuro). Ambas as regras ainda são pendência técnica, não implementadas. |
| [[Autorizacao Inicial Nao Segue o Padrao Multi-Conta (Sem Prefixo MB SV)]] | bug_conhecido | ativo | 26/08/2026 | Achado real ao estudar Autenticação e Autorização: `autorizacao_inicial.py` lê/grava `.env` sem prefixo de conta (`CLIENT_ID`, `ACCESS_TOKEN`), enquanto `gerenciador_token.py` exige prefixo (`MB_ACCESS_TOKEN`/`SV_ACCESS_TOKEN`). Rodar `autorizacao_inicial.py` hoje não resolveria uma reautorização de nenhuma das 2 contas, mesmo sendo a solução recomendada pelo próprio `gerenciador_token.py` quando a renovação falha de vez. Só afeta reautorização completa (raro); dia a dia não é afetado. Decisão explícita: não corrigir agora. |
| [[Achados Reais na Configuracao dos Aplicativos Mercado Livre (Magazine e Samvale)]] | descoberta | ativa | 26/08/2026 | Teste real via `teste.py` (código documentado em [[Gerencie seu Aplicativo na API do Mercado Livre]]) em `GET /applications/$APP_ID`, nas 2 contas. `max_requests_per_hour: 18000` nas 2. **Achado importante**: `callback_url`/`notifications_callback_url` das 2 contas apontam pra `webhook.site` (mesma URL de teste no `callback_url` das 2!), mas os apps já estão inscritos em lista extensa de tópicos reais de notificação — ML já manda evento de verdade pra um lugar que ninguém lê; bloqueia o plano futuro de webhook até isso ser trocado por endpoint real. Confirmado sem escopo `urn:mp:...` nas 2 (prazo Mercado Pago não afeta). 2 diferenças reais entre as contas, sem explicação/correção por ora: `sandbox_mode` (False na Magazine, True na Samvale) e escopo não-idêntico (Samvale tem 4 escopos a mais). Magazine reaproveita app legado "ML Analytics HUB" (criado 24/06/2026); Samvale tem app dedicado, novo (criado 05/08/2026). As 2 contas confirmadas saudáveis (não bloqueadas/desabilitadas) — descarta essa causa de 403. **Atualizado 23:00** com decodificação da doc "Permissões funcionais": tabela mapeando as 8 categorias nomeadas do DevCenter pros escopos reais dos 2 apps — dá nome ao escopo extra da Samvale (`urn:ml:mktp:metrics` = "Métricas do negócio") e mostra que 5 categorias (Comunicação, Publicidade, Vendas e Envios, Promoções, Faturamento) estão concedidas com leitura E escrita nas 2 contas, sem nenhum uso hoje. |

| [[Abandonar Excel Como Visualizacao Final]] | decisao | ativa | 10/07/2026 | Decisão de abandonar Excel como tela final de consulta do ML Analytics HUB — a árvore real (SKU→Página de Catálogo→Base→Catálogo, profundidade variável) não cabe em planilha; Sistema Interno (Django) vira a camada de modelagem/apresentação via banco relacional (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Classificacao de Anuncio Usa Apenas 2 Campos]] | decisao | ativa | 10/07/2026 | Classificação de anúncio (Base/Catálogo/Simples) definida usando só `catalog_product_id` e `catalog_listing`, sem depender de `item_relations` — validado contra caso real (SKU F7908050719121.001, 21 MLBs, 7 pares), inclusive catálogos órfãos sem par declarado (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Filtrar Fosseis De Migracao Na Camada De Consumo]] | decisao | ativa | 10/07/2026 | Decisão de filtrar MLBs "fósseis" (tag `variations_migration_source`) só na camada de consumo Django, nunca na coleta bruta — resolveu sozinho divergência de contagem de 5.831 para 5.724 anúncios (-107 MLBs excluídos) (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Nao Rastrear Historico Catalogo Encerrado Via Family Id]] | decisao | ativa | 10/07/2026 | Decisão de não rastrear histórico de catálogo encerrado via `family_id` — teste real mostrou que só 30 de 66 (45%) catálogos órfãos encerrados tinham correspondência por `family_id`, cobertura baixa demais para justificar a complexidade (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Nomenclatura Base Catalogo Simples Pagina De Catalogo]] | decisao | ativa | 10/07/2026 | Fixada a nomenclatura Página de Catálogo / Anúncio Base / Anúncio de Catálogo / Anúncio Simples para eliminar a ambiguidade da palavra "Catálogo" sozinha, descartando termos como "Avulso" e "Anúncios Competidores" (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Cache Nao Distingue Erro De Rede De Erro Real Da Api]] | bug_conhecido | ativa | 11/07/2026 | O cache de performance grava falha de rede (sem código HTTP) como se fosse erro definitivo da API, contaminando o resultado do MLBU pelo resto da execução — 1 falha real de DNS apareceu 7 vezes cacheada; falta só cachear quando houver HTTP real (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Fosseis de Migracao no Hub de Anuncios]] | Bug Conhecido | Resolvido | 12/07/2026 | MLB "fóssil" (MLB2751408939) apareceu no Hub com 48 variações fantasma, todas closed, tag `variations_migration_source` do próprio ML (107 MLBs no total) — corrigido com campo `eh_fossil_migracao` excluído sempre, resolvendo divergência de 383/107 unidades (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Roster De Campanha Sem Paginacao Implementada]] | bug_conhecido | ativa | 10/07/2026 | `coletar_promocoes.py` faz só 1 chamada por campanha e não segue o cursor `searchAfter`, trazendo dado truncado em campanhas grandes (ex: P-MLB17647056, 72 de 463 itens) — falta implementar o loop de paginação (nota migrada do LEGADO, tag `Vindo_do_Legado`). |

## Coleta_de_Dados

| Nota | Tipo | Status | Data | Resumo |
|---|---|---|---|---|
| [[Migracao dos Scripts Consumidores (buscar_mlbs e buscar_detalhes) e Pipeline de Popular Banco]] | checkpoint | em_andamento | 13/08/2026 | **Atualizado 27/08/2026, 07:31**: plano de 5 pontos **FECHADO** — commit `602d288` confirmado sincronizado com o GitHub (nada perdido na troca de computador) e base inteira do ponto 05 rodada na Magazine (1676 SKUs, 100% processados, 0 erros, 3280,5s), seguida de `popular_banco --empresa MAGAZINE` de novo — `QUALIDADE`/`COMPETICAO` agora gravam no banco pra base real, não só o SKU de teste. Magazine tem hoje o pipeline dos 5 pontos rodando ponta a ponta com dado de produção real, refletido na tela do site. Único item em aberto: rodar a mesma base inteira na Samvale — decisão explícita do usuário foi deixar pra depois (tempo de execução), sem risco técnico (código já commitado e validado lá também com 1 SKU). *Atualizado 26/08/2026, 16:35 — ÚLTIMA ATUALIZAÇÃO ANTES DE TROCA DE COMPUTADOR*: ponto 05 (`buscar_dados_sku_completo.py`) fechado — **plano de 5 pontos 100% migrado e testado**. Só `encontrar_fecho_transitivo()` precisou ser migrada de verdade (pro mesmo local canônico do ponto 04, `mercado_livre/funcoes_auxiliares/classificacao_catalogo.py`) — decisão embasada num bug real já documentado no código (`VariacaoAnuncioMercadoLivre.sku_ml`: variações aparecem com o SKU do pai), que tornaria o agrupamento via banco não confiável pra essa finalidade. `CAMINHO_QUALIDADE` (pendência desde 13/08) finalmente resolvida via `caminho_dados_completos_por_sku()`; `QUALIDADE`/`COMPETICAO` religadas em `popular_banco.py` — só `PROMOÇÕES ML` segue comentada (assunto pausado à parte). Testado com 1 SKU por empresa (Magazine: 19 MLBs, 7 base/6 simples/6 catálogo, todos os pares Base↔Catálogo conferidos batendo por `mlbu`; Samvale: 1 SKU, confirmado funcionando) e validado **visualmente na tela real do site** (gauge de qualidade + status de competição aparecendo), não só o dado chegando no arquivo. **Atenção crítica**: esse diff foi testado no computador do usuário mas AINDA NÃO FOI COMMITADO nem enviado pro GitHub (confirmado por `git fetch` — remoto segue no commit do ponto 04) — prioridade #1 ao retomar. Falta só: commitar, e rodar a base inteira (`buscar_dados_sku_completo` sem `--skus`, 1h30+, nas 2 empresas) + `popular_banco` de novo. *Atualizado 26/08/2026, 15:42*: ponto 04 (`classificar_por_sku.py`) fechado — mas virou eliminação de duplicação, não migração: a regra de classificação já existia, reimplementada à parte em `importar_anuncios_ml.py`; unificada agora em `mercado_livre/funcoes_auxiliares/classificacao_catalogo.py` (`classificar_catalogo()`), local padronizado pela convenção `<app>/funcoes_auxiliares/` já usada em todo o repo. Testado de verdade no shell (não só `manage.py check`): identidade de função confirmada + 3 exemplos reais batendo com a regra. Decisão de sequência revisada pelo usuário: banco populado agora, sem esperar o ponto 05 (que sozinho leva 1h+) — religadas em `popular_banco.py` as 2 das 5 etapas de ML que só dependiam de `detalhes_mlbs.json` (`ANUNCIOS ML`, `DIMENSÕES DECLARADAS ML`); as outras 3 seguem comentadas (`QUALIDADE`/`COMPETICAO` dão `NameError` sem o ponto 05 — `CAMINHO_QUALIDADE` nunca foi declarado). Validado com execução real, 0 erros, nas 2 empresas: Magazine (5640 anúncios, 5906 variações, cadeia 02→03→04 consistente, 0 órfãos) e Samvale. Achados de qualidade de dado expostos (não são bugs da migração): anúncios sem produto no ERP, divergências de dimensão de envio, 4 asserts de margem em Grade — registrados como pendência separada. 3 dos 5 pontos prontos; falta só ponto 05 (`buscar_dados_sku_completo.py`). *Atualizado 26/08/2026, 11:50*: ponto 03 do plano de 5 pontos (`buscar_detalhes.py`) concluído e validado nas 2 empresas — CSV removido (só JSON), retomada de progresso mantida e melhorada (só apaga se terminar sem erro), mesmo padrão de console em blocos do ponto 02 reaproveitado (agrupado por posição sequencial, sem dimensão "status" aqui), e caminho de `detalhes_mlbs.json` corrigido também no lado consumidor (`importar_dimensoes_declaradas_ml.py`, constante virou função por empresa). Magazine: 5906 registros (5640 MLBs, 100%, 0 erros), 165,3s. Samvale: 3545 registros (3280 MLBs, 100%, 0 erros), 86,8s. Sem gargalo nos 2 lados. *Atualizado 26/08/2026, 11:11*: ponto 02 (`buscar_mlbs.py`) concluído e validado com chamada real nas 2 empresas (Magazine: 5640 MLBs, 132,9s; Samvale: confirmado funcionando). App novo `integracao_mercado_livre/` criado, espelhando `api_sysemp`/`integracao_sysemp`. 2 correções reais em `cliente_api.py` (log por script, `propagate=False`). *Atualizado 25/08/2026, 11:58*: achado grande — o app `mercado_livre/` já existe e está maduro (11 models, 22 migrations, 5 importadores em POO já prontos); só falta migrar a "metade A" (busca na API + geração dos 2 `.json`), não o consumo. Schemas dos 3 `.json` confirmados; achado novo: campo `mlbu` existiu no banco e foi removido em 05/07/2026, decisão de reintroduzir ainda pendente. |

| [[Tutorial - Comandos de Coleta de Dados da API do Mercado Livre (buscar_mlbs, buscar_detalhes, buscar_dados_sku_completo)]] | tutorial | ativa | 26/08/2026 | Tutorial hiper-didático explicando os 3 comandos acima pra quem vai rodar (não construir): o quê cada um faz, por quê existe, e o que se obtém — com números reais validados (Magazine 5640 MLBs → 5906 detalhes → 19 MLBs no teste de SKU) e exemplo de ponta a ponta terminando em `popular_banco`. |

## Referencia_API

> [!important] Leia antes de tudo: [[Metodologia de Analise da Documentacao da API do Mercado Livre]]
> Regra que define o propósito real deste estudo — **não é auditoria** do código atual, é construção de conhecimento completo da API (uso seguro/otimizado/eficiente/validado, estrutura geral, e capacidade de cada endpoint individualmente), pra trabalhar bem tanto com o que já existe quanto com o que ainda vai ser criado.
>
> Sessão pausada em 27/08/2026, 00:20 — pendências, decisões em aberto e por onde retomar estão em [[Checkpoint — Estudo da API do Mercado Livre (Sessao 26-27_08_2026)]].

Estudo estruturado em 2 camadas da API do Mercado Livre, iniciado em 26/08/2026: **camada superior** (regras que valem pra qualquer chamada, não importa o endpoint — boas práticas, autenticação, erros comuns) e **camada de endpoint** (contrato técnico de 1 recurso específico por vez). Diferente de `Cliente_HTTP/` (que documenta decisão de implementação do nosso código) e `Coleta_de_Dados/` (que documenta nossos scripts) — aqui o assunto é a API do Mercado Livre em si, como fonte de conhecimento reaproveitável por qualquer contexto futuro.

| Nota | Tipo | Status | Data | Resumo |
|---|---|---|---|---|
| [[Metodologia de Analise da Documentacao da API do Mercado Livre]] | regra | ativa | 27/08/2026 | Regra de como pensar ao ler qualquer doc do ML: 3 eixos (uso seguro/otimizado/eficiente/validado, estrutura geral, capacidade de cada endpoint) — nunca descartar conteúdo por "não é leitura" ou "não usamos hoje". Nasceu de 2 casos reais de descarte indevido (doc "Publicar produtos", doc "Validador de Publicações"). Leia antes de qualquer outra nota de `Referencia_API/`. |
| [[Checkpoint — Estudo da API do Mercado Livre (Sessao 26-27_08_2026)]] | checkpoint | em_andamento | 27/08/2026 | Estado da sessão de estudo da API do ML — código confirmado limpo (só leitura, nada commitado); 6 pendências abertas (`TIPO_LIST` incompleto, 2 valores de status não testados, unidade de dimensão a confirmar, `unified_units` sem explicação, 2 docs de escrita pendentes de reprocessamento sob a metodologia nova). |
| [[Boas Praticas para Uso da Plataforma do Mercado Livre]] | conceito | ativa | 26/08/2026 | 1ª nota da camada superior. Doc oficial (atualizada 30/12/2025) — regras de mensagem automática/etiqueta/clonagem/catálogo não nos afetam hoje (só lemos dado); seção Web Crawler afeta: scraping proibido (já cumprimos, só usamos a API), restrição de IP do app recomendada (não verificado), reação a erro 429 (implementada, `cliente_api.py`). Achado real: o cliente do Mercado Livre só tem a peça reativa (backoff) do padrão de proteção do projeto — falta o espaçador proativo que [[Padrao de Robustez para Clientes de API Externa]] exige. |
| [[Consideracoes de Design da API do Mercado Livre]] | conceito | ativa | 26/08/2026 | 2ª nota da camada superior. Formato JSON/JSONP (não usamos), formato padrão de erro (`message`/`error`/`status`/`cause`) e achado real: `cliente_api.py` não lê esses campos, só embrulha `resposta.text` cru. `attributes=` pra reduzir resposta existe mas não é usado em `buscar_detalhes.py` — sinalizado pelo usuário como estudo futuro importante. Paginação normal (`limit`/`offset`, padrão 0/50) documentada em contraste com o `scan` já estudado no endpoint de `buscar_mlbs`. |
| [[Erro 403 (Forbidden) da API do Mercado Livre]] | conceito | ativa | 26/08/2026 | 3ª nota da camada superior. "Validador de Publicações" (tópico anterior) foi descartado por ser só sobre criar anúncio; este afeta nossas chamadas de leitura de verdade. Achado real: formato de erro varia por tipo (403 traz `code`, às vezes sem `message`/`cause`) — motivou ajuste na Regra 1 de [[Tratamento Detalhado e Relatorio Estruturado de Erros de Chamada a API do Mercado Livre]]. 7 causas mapeadas (app bloqueado, permissão, usuário inativo, IP bloqueado, scope mal configurado, token de conta errada, dado não validado), cruzadas com decisões já tomadas (IP não restringido, 2 contas com token separado). **Atualizado 23:00** com sub-caso específico da doc "Permissões funcionais": formato de erro próprio pra falta de permissão funcional (`code: PA_UNAUTHORIZED_RESULT_FROM_POLICIES`, campo novo `blocked_by: PolicyAgent`), incorporado à Regra 1. |
| [[Autenticacao e Autorizacao na API do Mercado Livre]] | conceito | ativa | 26/08/2026 | 4ª nota da camada superior. OAuth2 fluxo Server Side + PKCE explicado, confirmado batendo com o código (`gerar_pkce()`, `S256`). Ciclo de vida do token cruzado com o real: 6h de duração e regra "renovar só perto de expirar" batem exatamente com `DURACAO_TOKEN_SEGUNDOS`/`RENOVAR_ANTES_SEGUNDOS`; rotação do `refresh_token` (uso único) confirmada implementada corretamente. 9 códigos de erro de OAuth catalogados. Achado real (grave): `autorizacao_inicial.py` não segue o padrão multi-conta — ver [[Autorizacao Inicial Nao Segue o Padrao Multi-Conta (Sem Prefixo MB SV)]]. **Atualizado 22:46** com doc complementar "Recomendações de Autenticação e Token": confirmado certo (parâmetros de `/oauth/token` sempre no body, nunca querystring); gap real de baixa prioridade (`state` ausente na URL de autorização, sem correção agora — decisão do usuário); lembrete pro futuro (validação de origem de webhook, junto do dia em que o `callback_url` real for configurado). |
| [[Gerencie seu Aplicativo na API do Mercado Livre]] | conceito | ativa | 26/08/2026 | 5ª e última nota da camada superior — a única que gerou teste real (ver descoberta abaixo). Mapeados os 5 endpoints de gestão do app (detalhes, apps por usuário, grants com 3 estados, revogar, métrica de consumo). Prazo Mercado Pago confirmado que não afeta (com prova real). `max_requests_per_hour` explicado como o dado que faltava na decisão do espaçador proativo. Métrica de consumo testada e confirmada como pouco útil pra debug imediato (D-1, só 4 chamadas no relatório porque todo o trabalho de hoje ainda não foi refletido) — reforça a Regra 2 como abordagem principal. |

| [[Classificacao De Promocoes Com Rebate Vs Sem Rebate]] | conceito | ativa | 11/07/2026 | Dos 7 tipos de campanha ativos na conta, só SMART tem rebate do ML confirmado com dado real (82 casos, 0,3%-4%); DEAL, PRICE_DISCOUNT e LIGHTNING são 100% bancados pelo vendedor; UNHEALTHY_STOCK, BANK e PRICE_MATCHING existem mas sem rebate populado em item real (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Fossil De Migracao]] | conceito | ativa | 10/07/2026 | Fóssil de migração é MLB residual identificado pela tag `variations_migration_source`, sempre `status: closed`, que carrega variações históricas irreais (ex: MLB2751408939 com 48 variações) e infla contagens agregadas de anúncios se não filtrado na camada de consumo (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Pagina De Catalogo]] | conceito | ativa | 10/07/2026 | Página de Catálogo é a entidade do ML (identificada por `catalog_product_id`, não é MLB) onde múltiplas Bases e Anúncios de Catálogo competem, numa hierarquia 1 SKU → N anúncios → 1 Página → N Bases → N Catálogos (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Rebate E Boost]] | conceito | ativa | 10/07/2026 | Dois mecanismos de cofinanciamento do ML em promoções: rebate clássico (`benefits.type=REBATE`) e `boosted_offer` (novo, 2026, campo no item) — testado em 106 MLBs, 37,7% tinham rebate/boost, presente sobretudo em anúncios ativos, quase ausente em pausados (nota migrada do LEGADO, tag `Vindo_do_Legado`). |

### Endpoints

Camada de endpoint — contrato técnico de 1 recurso específico por vez, diferente da tabela acima (regra geral, independe do endpoint).

| Nota | Tipo | Status | Data | Resumo |
|---|---|---|---|---|
| [[Recurso Items (GET) — Leitura de Detalhe de Anuncio na API do Mercado Livre]] | conceito | ativa | 26/08/2026 | 1ª nota da camada de endpoint. Endpoint exato: `GET /items?ids=` (multiget, resposta em lista `{code, body}`, usado por `buscar_detalhes.py`, lotes de 20) — **atualizada 23:40** com callout explícito no topo distinguindo esse endpoint de `GET /items/$ITEM_ID` (item único, formato de resposta diferente) e do endpoint de busca (`buscar_mlbs.py`), pra eliminar ambiguidade (pedido explícito do usuário). Cobre só a parte de **leitura** — parte de criação/publicação da doc original ("Publicar produtos") descartada por decisão do usuário, mesmo critério do "Validador de Publicações". Achados: `condition` sendo substituído por `item_condition` (nosso código lê o campo antigo, retrocompat ativa, sem ação agora); `tags` já coletado sem decodificar (tabela completa de ~40 tags registrada como dicionário); confirmado que `extrair_sku()` já segue a boa prática (`SELLER_SKU` antes de `seller_custom_field`); 206/`X-Content-Missing` explicado (`location`/`geolocation`/`seller_address`) e confirmado que nenhum dos 3 é usado hoje pelo nosso pipeline. |
| [[Endpoint Users Items Search (Scan) — Busca Completa de MLBs por Vendedor na API do Mercado Livre]] | conceito | ativa | 26/08/2026 | 2ª nota da camada de endpoint. Endpoint exato: `GET /users/{user_id}/items/search` com `search_type=scan` (usado por `buscar_mlbs.py`, dentro de `buscar_mlbs_varrida()`) — nota abre com callout explícito distinguindo esse endpoint de 4 outros "vizinhos" citados na mesma doc de origem, pra não haver ambiguidade. **Achado grande, decisão adiada pro próximo dia útil**: `TIPO_LIST` do código só cobre 2 dos 7 `listing_type_id` documentados (`gold_pro`/`gold_special`; faltam `gold_premium`/`gold`/`silver`/`bronze`/`free`) — MLB nesses 5 tipos nunca é encontrado por nenhuma das 168 varridas, risco real e silencioso, sem correção decidida ainda. **Achado a testar**: 2 valores de `STATUS_LIST` (`under_review`/`payment_required`) não aparecem no exemplo de filtro da doc — pendência de teste ao vivo, sem conclusão. Confirmado: `scroll_id` já rotaciona certo no código (apesar de texto ambíguo na doc), expiração de 5 min agora oficial, risco baixo dado o tempo real de execução (132,9s pra 168 varridas). Descartado explicitamente: `attributes=` e `available_quantity` em faixa não se aplicam a este endpoint (só ao endpoint irmão de multiget). **Atualizada 23:55** com filtro adicional `?tags=incomplete_technical_specs` (achado numa doc diferente, sobre Atributos). |
| [[Sistema de Atributos de Item na API do Mercado Livre]] | conceito | ativa | 26/08/2026 | 3ª nota da camada de endpoint — tratada com peso extra por decisão explícita do usuário ("é aqui onde eu extraio o máximo de dados sobre um anúncio"). Endpoint principal: `GET /categories/{category_id}/attributes` (schema de atributo por categoria), mais 4 endpoints irmãos (`technical_specs/input`/`output`, `attributes/conditional`, `catalog_domains/.../top_values`). Cobre os 5 `value_type`, as 17 tags de comportamento do atributo (com matriz de exclusão/implicação), 2 fontes de obrigatoriedade (bloqueia publicação vs só penaliza exposição), atributo condicionalmente obrigatório (exemplo real de cerveja), atributos de dimensão de pacote (`SELLER_PACKAGE_*`, cruzado com nosso código — ver nota de Items), mecanismo de N/A (achado: não conseguimos ver esses valores hoje, `include_internal_attributes=true` não é usado), `top_values` (ferramenta de descoberta, ideia futura), e modificar/remover atributo via PUT. Callout explícito: "tags de atributo" (schema) ≠ "tags de item" (qualidade/logística), mesma palavra, conceitos diferentes. Ponto em aberto: campo `unified_units` sem explicação na doc. |
| [[Aumento De Preco Remove Item De Campanhas Cofinanciadas Marketplace E Volume]] | regra | ativa | 11/07/2026 | Aumentar o preço de um item retira-o automaticamente e de forma permanente de campanhas MARKETPLACE_CAMPAIGN e VOLUME, sem aviso prévio — regra documentada só para esses 2 tipos, sem confirmação de que valha para SMART/PRICE_MATCHING, os realmente usados na prática (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Boosted Offer Nunca Confirmado Na Pratica]] | duvida | ativa | 11/07/2026 | Analisadas 395 promoções reais (76 MLBs): `boosted_offer` nunca apareceu como `true` e seus campos associados nem constam na amostra, enquanto o rebate clássico foi confirmado em 82 casos (~21%) — mecanismo novo de 2026 segue só documentação nesta conta (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Campanha Smart Continua Ativa Mesmo Com Anuncio Pausado]] | descoberta | ativa | 11/07/2026 | Diferente da documentação, campanhas SMART podem continuar `status: started` (com desconto ativo) mesmo após o anúncio ser pausado — confirmado em 4 casos reais de 2 campanhas; PRICE_DISCOUNT e DEAL nunca apareceram `started` em anúncios pausados (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Catalogo Pode Divergir Entre Variacoes Do Mesmo Mlb]] | duvida | ativa | 10/07/2026 | Dúvida em aberto se `catalog_product_id`/`catalog_listing` podem divergir entre variações do mesmo MLB — teste com MLB3321138767 (20 variações) não confirmou nem refutou, pois nenhuma estava em catálogo; pista indireta sugere que o caso é raro (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Catalogo Sem Estoque Ou Pausado Mantem Classificacao]] | descoberta | ativa | 10/07/2026 | Catálogo sem estoque (1.052 casos) ou pausado mantém `catalog_listing=True` intacto — a classificação estrutural só muda quando o anúncio é genuinamente encerrado; falta de estoque/pausa afeta só a competição (`price_to_win` vira `not_listed`), não o tipo (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Divergencia Entre 3 Fotos Da Api E 5 Fotos Da Equipe]] | duvida | ativa | 10/07/2026 | Equipe acredita que o ML exige 5 fotos para pontuação máxima, mas o critério `UP_PICTURES_QUANTITY_MIN` pede literalmente "pelo menos 3" — divergência não resolvida, motivou a prática de sempre exibir o texto bruto da API ao lado da interpretação própria (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Endpoint Performance So Aceita MLBU Nao Aceita MLB]] | descoberta | ativa | 10/07/2026 | O endpoint `/user-product/{id}/performance` rejeita MLB com HTTP 404, só aceita MLBU — confirma que não há atalho técnico para obter performance exclusiva de um MLB de Catálogo contornando o compartilhamento de MLBU com o Base (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Endpoint Promocoes Rejeita Anuncio Encerrado Com Http 400]] | descoberta | ativa | 10/07/2026 | `GET /seller-promotions/items/{MLB}` rejeita todo MLB com `status: closed` com HTTP 400 explícito ("Item status is not allowed") — confirmado em 100% de 40 MLBs closed testados; scripts de coleta podem pular MLBs closed sem chamar o endpoint (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Endpoints De Promocoes Ainda Nao Explorados]] | duvida | ativa | 11/07/2026 | Lista de pendências de leitura no bloco de Promoções: detalhe de campanha, exclusion-list do vendedor/item, candidates e offers (só acessíveis via notificação) — exclusion-list apontada como prioridade por poder revelar bloqueio silencioso de campanhas automáticas (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Faixas De Corte Da Cor De Reputacao De Experiencia]] | duvida | ativa | 11/07/2026 | Hipótese não confirmada oficialmente das faixas de `reputation.color`: -1 cinza, 0-30 vermelho, 31-65 laranja, 66-100 verde — sustentada por 72 amostras reais (-1, 30, 65, 100), sem nenhum valor testado nas faixas intermediárias 31-64/67-99 (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Falta Endpoint De Vendas Por Data Para Conversao Em Janela]] | duvida | ativa | 11/07/2026 | `sold_quantity`/visitas gerou conversões acima de 100% (até 306%) em anúncios antigos porque `sold_quantity` é cumulativo desde a criação enquanto visitas cobre só 2 anos — falta endpoint de Pedidos/Vendas com data para janelas casadas (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Imagem Alta Resolucao Via Sufixo F Na Url]] | descoberta | ativa | 10/07/2026 | `pictures` da API vem em baixa resolução (500x500); trocando o sufixo do arquivo de `-O` para `-F` obtém-se a versão 1200x1200, confirmado por download real — convenção não documentada oficialmente, pode mudar sem aviso (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[MLBU Compartilhado Entre Base E Catalogo Pareado]] | descoberta | ativa | 10/07/2026 | Confirmado com 7 de 7 pares reais do SKU F7908050719121.001 que Base e Catálogo pareado compartilham o mesmo MLBU (retificando afirmação anterior errada, não verificada) — variações físicas diferentes têm MLBU próprio, distinto (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Mapa Completo Do Endpoint Price To Win Em Escala]] | descoberta | ativa | 11/07/2026 | 1.711 chamadas reais a `price_to_win` confirmaram `sharing_first_place` (11) e `listed` (2) como valores reais da API e revelaram motivos não documentados — consistência exata de 1.203 casos entre 3 indicadores diferentes de "não opt-in" (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Mapa Completo Do Endpoint Promocoes Por Item]] | descoberta | ativa | 11/07/2026 | Mapeados 395 registros reais de promoções por item (76 MLBs): 89% são só "candidatas" (não ativadas), rebate clássico varia de 0,3% a 4% (média 1,7%), e erro 400 em itens closed confirmado em 30 de 30 casos (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Mapa Completo Do Endpoint Purchase Experience Integrators]] | descoberta | ativa | 11/07/2026 | Mapeamento de 72 respostas reais de Experiência de Compra: campo `reasoning` recombina 4 dimensões fixas (reclamações, cancelamentos, atrasos, opiniões) sem exposição estruturada; 19 de 72 MLBs (todos closed) ainda respondem na estrutura antiga (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Mecanismo Contabil Do Rebate Classico Nao E Garantido Pela Documentacao]] | duvida | ativa | 11/07/2026 | A fórmula usada na prática trata rebate clássico como abatimento direto da comissão do ML, validada pelo dono do negócio — mas só o mecanismo `boosted_offer` (nunca observado nesta conta) tem esse mecanismo contábil garantido explicitamente pela documentação (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Migracao Parcial Da Experiencia De Compra Para User Product]] | descoberta | ativa | 11/07/2026 | Endpoint "por item" de Experiência de Compra é redirecionado (HTTP 302) para a estrutura nova de User Product em anúncios active/paused (53 de 72 casos), mas os 19 anúncios closed continuam na estrutura antiga — chamar só `experiencia_up` basta para ativos/pausados (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Nivel Basica Nunca Observado Em Performance]] | descoberta | ativa | 11/07/2026 | Em 2.804 respostas válidas de performance (6.489 MLBs coletados), nenhum score ficou abaixo de 53 e o nível "Básica" nunca apareceu — só Profissional (97%) e Satisfatória (3%), sem explicação confirmada entre padrão de qualidade real ou viés de amostra (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Notificacoes Webhook Como Otimizacao Futura Bloqueada Por Falta De Servidor]] | duvida | ativa | 14/07/2026 | A única forma real de otimizar a Fase 2 de Promoções (~5.615 chamadas) é usar Webhooks dos tópicos "public candidate"/"public offers" em vez de perguntar periodicamente — bloqueado hoje por falta de servidor com URL pública (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Preco Alvo E Rebate Disponiveis Antes De Ativar Promocao]] | descoberta | ativa | 11/07/2026 | Para promoções com rebate, preço-alvo e percentuais já vêm 100% preenchidos mesmo em `status: candidate` (confirmado em 82 de 82 casos) — permite calcular margem antes de ativar; um mesmo item pode ter múltiplas ofertas concorrentes com divisões de rebate diferentes (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Roster De Campanha Nao E Confiavel Para Uso Programatico]] | descoberta | ativa | 14/07/2026 | Roster de campanha traz item duplicado (recálculo durante paginação) E omite item realmente participante (MLB4959724082 started não apareceu) — decisão de parar de usar a Fase 3 na coleta de produção, mantendo só Fase 1+2 (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Roster De Campanha Usa Searchafter Dentro De Paging]] | descoberta | ativa | 10/07/2026 | O cursor de paginação do roster de campanha chama-se `searchAfter` e fica dentro do objeto `paging`, não na raiz — confirmado que rosters estão truncados sem paginar (ex: 72 de 463 itens); `paging.total` às vezes vem menor que o já coletado (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Roster De Campanhas Smart Tem Duplicatas Reais Durante Paginacao]] | descoberta | ativa | 14/07/2026 | Após implementar paginação via `searchAfter`, 31 de 39 campanhas vieram com mais itens coletados que o total declarado — confirmado que o excesso é 100% duplicata real (dedup bate exato com o total), quase exclusivo de campanhas SMART (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Score De Performance Do Catalogo E Do Base Compartilhado]] | descoberta | ativa | 10/07/2026 | Score de performance exibido para um Anúncio de Catálogo é, na prática, o mesmo dado do Anúncio Base pareado (retorno idêntico byte a byte, bucket ITEM sempre aponta para o MLB do Base) — não há forma de obter performance exclusiva do Catálogo (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Sku Repetido Em Variacoes Nao E Bug]] | descoberta | ativa | 10/07/2026 | Bug reportado (SKU repetido entre variações do mesmo MLB) foi refutado — o campo `attributes` não existe dentro do objeto de variação da API; 43 de 46 variações testadas (93%) simplesmente não têm `seller_custom_field` preenchido, ausência real de dado (nota migrada do LEGADO, tag `Vindo_do_Legado`). |
| [[Variacoes Nativas So Existem Em Anuncios Encerrados]] | descoberta | ativa | 10/07/2026 | Variações nativas do ML existem quase exclusivamente em anúncios `status: closed` (100% dos ~490 registros com `tem_variacoes=True`) — resíduo de modelo abandonado; a operação hoje cadastra cada combinação como MLB próprio com SKU próprio (padrão F+EAN+.001) (nota migrada do LEGADO, tag `Vindo_do_Legado`). |

## Qualidade_Visual_de_Anuncios

Nova frente aberta em 27/08/2026, 20:23 — foco do dia declarado pelo usuário: criar uma tela no sistema pra visualizar todas as fotos de um anúncio, espelhando a árvore já existente do Hub de Anúncios. Fase de idealização/discussão — nenhum código gerado ainda, por pedido explícito do usuário.

| Nota | Tipo | Status | Data | Resumo |
|---|---|---|---|---|
| [[Checkpoint — Tela de Visualizacao de Fotos dos Anuncios (Hub de Fotos)]] | checkpoint | concluido | 28/08/2026 | **Tela concluída e validada (28/08, 00:44)** — usuário confirmou: "ficou realmente excelente". Nota viva de referência pra esta frente. Objetivo: tela igual à árvore do Hub de Anúncios (EAN/SKU → Base/Catálogo → Simples), mostrando por folha todas as fotos do anúncio em ordem real, em vez dos dados de preço/qualidade/competição atuais. **Decisões fechadas (21:33)**: toda folha, sem exceção (mesmo variação, <1% da base), mostra o pool completo do MLB; fonte do dado exige rodar `buscar_detalhes.py` de novo com extração ajustada (o `detalhes_mlbs.json` atual descarta `pictures` em memória, não é reaproveitável); campos a capturar por foto definidos (`id`/`url`/`secure_url`/`size`/`max_size`/`quality`). **Verificado com script de exploração real** (`scripts_exploracao_ML/investigar_retorno_bruto_item.py`, novo, só leitura): estrutura de `pictures` confirmada com 1 MLB real, achado não esperado de um campo `health` (nível raiz, candidato a origem do `UP_PICTURES`), e tags de imagem (`good_quality_picture` etc.) já capturadas cru mas ainda não decodificadas. **Onde guardar as fotos, decidido (21:48)**: campo `fotos` (`JSONField`) em `AnuncioMercadoLivre`, mesmo padrão já validado por `item_relations` no mesmo modelo — 3 pontos de mudança mapeados (`buscar_detalhes.py`, model, `importar_anuncios_ml.py`), ainda não implementados. Nota de design pro futuro: um eventual botão de "marcar foto pra refazer" não pode morar dentro desse JSON (é sobrescrito por inteiro a cada reimportação) — precisaria de tabela própria separada; decisão explícita do usuário por ora é só visualização, sem botão. **Árvore e visual decididos (22:03), implementados e funcionando (22:30)**: reaproveita `montar_estrutura_de_sku()`/`carregar_variacoes_por_sku()` direto; cabeçalho do card mantido igual; miolo em grade fixa de 6 colunas. Código aplicado (model+migração, `buscar_detalhes.py`, `importar_anuncios_ml.py`, `info_variacao()`, view/URL/menu novos, template+parcial+CSS novos) e base populada rodando os comandos de novo. **Feedback do usuário**: "toda a parte funcional até aqui funciona. Mas está horrível visualmente" — refino visual em andamento, por partes. **Rodada 1 do refino (23:02)**: 4 problemas reais (espaço vazio, numeração sobre a foto, falta de dado de tamanho, foto pequena) resolvidos trocando grid de colunas por `flex-wrap` com largura fixa (180px testado) + legenda com resolução por foto — validado em 2 larguras de tela (1366/1600px), tamanho de foto consistente nos 2 casos. **Visualizador modal/lightbox (23:38)**: fotos clicáveis, navegação anterior/próxima com wrap-around, cabeçalho + contador + legenda, corrigido borrão (troca `-O`→`-F`), tamanho do quadro (`vmin` + variável CSS única) e contraste/sobreposição de controles — aprovado pelo usuário ("ok agora ficou top"), mas refino geral segue não fechado. **Otimização de carregamento (28/08, 00:08)**: mantido `-O` na grade (medido ~3,9x mais leve que `-F`: 89kB/500px vs 344kB/1200px; com paginação de 25 folhas × ~10-12 fotos, `-F` em tudo custaria ~90-100MB por página vs ~25-27MB hoje) + preload em segundo plano (hover no quadradinho + abertura do modal) pro `-F` — delay de carregamento zerado, confirmado pelo usuário. Frente de otimização encerrada por ora. **Fechamento final (28/08, 00:44)**: cabeçalho do card reestruturado em 3 blocos (cabeçalho tingido/borda + corpo com a grade + rodapé com borda superior), reaproveitando padrões já existentes no CSS (`.hub-sku-header`/`.hub-sku-conteudo`, `.hub-filtros-acoes`); adicionado botão "Editar no ML" (ícone de lápis, ao lado do "Ver no ML" com ícone de olho), URL ainda placeholder (`href="#"` + `TODO` no template, a preencher quando disponível). Usuário declarou a tela **completamente pronta e validada**. Pendências remanescentes (não-visuais): URL real do "Editar no ML", utilidade do campo `quality`, campo `health`, tags de qualidade de imagem na tela ou não (ponto 5), vídeo (fora de escopo). |

## Relacionado

- [[Regra do Índice Obrigatório]]
- [[Padrao de Robustez para Clientes de API Externa]]
