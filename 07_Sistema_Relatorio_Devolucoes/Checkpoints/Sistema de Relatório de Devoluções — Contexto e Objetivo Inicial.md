---
tipo: checkpoint
dominio: 
status: em_andamento
criado: 01/09/2026
atualizado_em: 03/09/2026 07:23
relacionado: [Processo de Devolução de Produtos e os 3 Caminhos Possíveis, Arquitetura de Entrega do App de Devolução — PyInstaller Onedir, Loading HTML e Ícone de Bandeja, Definição do Núcleo de Engenharia Repositório, Reestruturação de Telas — Produtos como Tela Direta, Edição de Dados do Produto Embutida no Catálogo, Geração do PDF de Devolução — xhtml2pdf, Sem Persistência e Fluxo por GET, Tutorial - Como Compilar e Testar o Sistema de Devolução (Com e Sem o .exe)]
---

# Sistema de Relatório de Devoluções — Contexto e Objetivo Inicial

**Resumo do estado atual**: o rascunho completo (catálogo de peças, telas de Produtos e Nova Devolução, geração de PDF) foi apresentado em 03/09/2026 e **100% aprovado**, rodando em modo desenvolvimento (`runserver`) numa rede local (LAN) — funcionou sem nenhum problema nesse modo. A partir de agora, o projeto deixa de ser prova de conceito e passa a ser desenvolvido como sistema real e robusto. O empacotamento como `.exe` segue sendo a única frente sem confirmação: 2 bugs reais de empacotamento foram encontrados e já têm correção proposta (ver linha do tempo, 03/09/2026), mas o build nunca chegou a ser recompilado com elas — o computador disponível tem HD muito lento, tornando cada ciclo de compilação do PyInstaller impraticável por ora. Não é "não funciona", é "ainda não foi possível testar direito".

> [!success] Rascunho 100% aprovado — projeto vira desenvolvimento real
> Apresentado e aprovado em 03/09/2026, 07:23, rodando via `runserver` em LAN. Pendência real restante: confirmar o `.exe` empacotado — 2 correções já diagnosticadas e propostas, ainda não testadas (HD lento do computador disponível impede o ciclo de build por ora).

## Linha do tempo

**01/09/2026, 19:21** — Mundo criado. Contexto inicial: o caminho 1 (venda comum) funciona bem, sem problema. Os caminhos 2 (venda como usado) e 3 (troca) geram a dor real — levantamento manual e desgastante do estado do produto (peças faltando, avarias, condição da embalagem, dados do cliente e do produto). Objetivo: resolver a dor dos caminhos 2 e 3, não do caminho 1.

**01/09/2026, 19:40** — Ideia inicial de solução: um sistema interativo onde, por produto, já se sabe de antemão quais peças ele deveria ter — o usuário seleciona foto(s) e relata o problema encontrado especificamente em cada peça, sem precisar memorizar nem consultar manual. Por trás, um catálogo de peças por produto, construído aos poucos (fotos + nome técnico de cada peça, produto por produto), crescendo na mesma velocidade que o sistema.

Restrições reais levantadas nesta rodada: usuário final não-técnico (sistema precisa ser "pronto pra uso", tipo app — usuário não interage com código nem com VSCode); projeto 100% separado do Sistema Interno V2 (repositório próprio, sem ligação com o Django atual); sem banco de dados SQL (restrição corrigida depois, ver 20:20); sem automação ou reconhecimento automático de peça via IA (fora de escopo, decisão firme do usuário).

Proposta inicial (ainda não validada) de modelo do catálogo: pasta do Windows com 1 subpasta por produto (contendo as fotos das peças) + 1 arquivo JSON por produto, com schema fixo, mapeando cada foto pro nome da peça e o nome técnico dela.

**01/09/2026, 19:49** — Refinamento do modelo de entrega: o usuário não quer abrir mão da qualidade visual e da robustez que o Django já entrega hoje. A visão é um "aplicativo" que o usuário executa, e que abre uma visualização tipo site — mesmo padrão de compartilhar por IP na LAN que já usam no Sistema Interno atual — então por fora parece um app simples, por dentro tem estrutura de site de verdade. Motivo prático: o setor de devolução fica em outro barracão, o dev não consegue estar presente o tempo todo, então o sistema precisa rodar sozinho pro usuário no dia a dia de uso. Clarificado à parte: a atualização do sistema continua sendo feita pelo dev presencialmente quando necessário — isso não é problema; o que precisa deixar de depender dele é só o uso diário, não a manutenção. Usuário confirmou o fechamento desta base: "acho que a base é essa, não tenho mais nada a comentar agora antes de começarmos a de fato pensar na implementação."

**01/09/2026, 20:20** — Início da implementação, em repositório próprio (novo repositório GitHub, Poetry + pyenv 3.12.10). Django instalado e projeto padrão (`core`) rodando via `manage.py runserver` — tela inicial do Django confirmada no navegador.

Ao rodar, apareceu aviso de migração pendente — esclarecido: o Django já vem, por padrão, com SQLite configurado pra 4 apps internos dele (`admin`, `auth`, `contenttypes`, `sessions`), independente de qualquer model próprio criado. Isso levou a uma **correção da restrição registrada em 19:40** ("sem banco de dados SQL"): a régua real do usuário é **praticidade**, não ausência de SQL em si — o que não pode acontecer é a complexidade de instalar/gerenciar algo como MySQL + MySQL Workbench. SQLite (zero instalação, 1 arquivo, já embutido no Python/Django) está liberado — inclusive como opção a reconsiderar pro próprio catálogo de peças, no lugar da proposta original de pasta+JSON (ainda em aberto, não decidido).

Testado e validado o requisito mais crítico do projeto ("se isso não funcionar, nada mais adianta"): um script (`launcher.py`, usando `waitress` + `webbrowser`) sobe o servidor Django e abre o navegador sozinho, sem comando manual do usuário. Empacotado com PyInstaller `--onefile` (console ainda visível de propósito) e funcionou de primeira.

**01/09/2026, 21:13** — Iteração completa até a arquitetura de entrega final, com todos os problemas reais encontrados no caminho e resolvidos — detalhe completo em [[Arquitetura de Entrega do App de Devolução — PyInstaller Onedir, Loading HTML e Ícone de Bandeja]] (nota de decisão nova, não duplicado aqui): tentativa de splash nativo com Tkinter esbarrou num bug real e não totalmente diagnosticado (`Tcl_AsyncDelete`); investigação do `agente_local` mostrou que ele nunca usou Tkinter (usa `pystray` + `Flask`, aviso visual é banner web, não janela nativa); pivô pra tela de carregamento em HTML puro, sem Tkinter; descoberta de que `--onefile` sempre tem atraso de extração antes de qualquer código rodar (sem correção possível); troca pra `--onedir`, resolvendo o atraso. **Testado com `--noconsole` de verdade, ponta a ponta, e funcionou**: `.exe` abre, mostra "Iniciando o sistema...", troca sozinho pro site real, ícone de bandeja com Abrir/Encerrar, sem console visível. Usuário confirmou: "essa será a maneira que iremos seguir o projeto todo" — arquitetura de entrega fechada como decisão, não mais experimento.

**01/09/2026, 22:48** — Decisão: o projeto passa a seguir o mesmo padrão de qualidade de engenharia do [[Definição do Núcleo de Engenharia Repositório]] (estrutura de arquivo/classe, modelagem de objeto, disciplina de testes, etc.), mesmo sendo repositório separado do Sistema Interno V2 — o repositório separado existe só por motivo operacional (não misturar experimentação com o sistema em produção), nunca como licença pra um padrão menor. Nota de definição do núcleo atualizada pra refletir esse escopo expandido. Aplicados os primeiros ajustes estruturais no código já escrito: cabeçalho de arquivo + comentário "Função Objetivo" em `launcher.py` e `devolucoes/views.py`, reorganização de imports em blocos, e trava de instância única (verifica se a porta já está em uso antes de subir um 2º processo).

**01/09/2026, 23:06** — Trava de instância única (`porta_ja_em_uso()` em `launcher.py`) testada e validada da forma real de uso: `.exe` recompilado (`pyinstaller --onedir --noconsole`) e testado por duplo clique, não via `manage.py runserver`/script solto. Abrir o `.exe` uma 2ª vez com o app já rodando não cria um 2º processo — confirmado funcionando corretamente.

**01/09/2026, 23:11** — Fechada a validação técnica ponta a ponta da 1ª tela real (formulário de nova devolução): 2 bugs de empacotamento corrigidos — `TemplateDoesNotExist` (faltava `--add-data` do PyInstaller pra pasta `devolucoes/templates`) e estático não servido (`waitress` serve o WSGI puro, nunca passa pelo `runserver` — resolvido com uma view própria em `core/urls.py` usando `django.contrib.staticfiles.views.serve`, mais `--add-data` pra `devolucoes/static`). Testado no `.exe` empacotado de verdade (não em `runserver`), com print confirmando visual idêntico ao mockup: formulário, bloco de produto e paleta de cores do Sistema Interno todos corretos.

**01/09/2026, 23:30** — Rejeitada a ideia de imagem explodida única por produto (complexa demais de gerar na prática) — o catálogo será mesmo foto individual por peça, cada uma em seu próprio "quadradinho" (nome, foto, quantidade esperada), incluindo um quadradinho futuro de "Adicionar peça" pro catálogo crescer aos poucos (guardado pra depois, fora de foco por ora).

Definido também, com validação de quem vai usar de verdade, o modelo de interação da conferência de peça na hora da devolução — cobre os 4 cenários reais (peça não veio, veio em quantidade menor, veio na quantidade certa mas danificada, veio tudo certo): o usuário só marca o que RECEBEU, e o sistema deduz por contraponto o que não veio (nunca o usuário marca "faltou" manualmente). Peça de quantidade 1 é um marcador simples (recebida/não recebida); peça de quantidade >1 (ex: 4 parafusos) vira um seletor de "quantos vieram" (0 até o total esperado), com o déficit calculado automaticamente. Toda peça marcada como recebida ganha um ícone de lápis pra anotação de condição (ex: "veio riscada") — texto livre por enquanto (sem tags fixas, fica pra uma otimização futura), valendo tanto pra peça de quantidade única quanto múltipla (sem distinção por ora). Usuário validou o conceito: "ficou muito fácil de usar... cada coisa no seu lugar, nada se sobrepõe a nada".

**02/09/2026, 01:06** — Estado real: **projeto longe de terminar**, esta entrada é só um corte de progresso, não um fechamento. Desde a última atualização (23:30), foi implementado (não só desenhado) o catálogo de peças: models `Produto` e `Peca` (Django/SQLite), com regra explícita do usuário confirmada no código — peça pertence a exatamente 1 produto, nunca compartilhada entre produtos (`ForeignKey` simples, sem N-pra-N). Corrigido de passagem um bug real que teria custado o cadastro de hoje: banco e fotos usavam `BASE_DIR`, que dentro do `.exe` aponta pra dentro de `_internal` — pasta inteira recriada a cada build. Agora, quando empacotado, banco e mídia vão para `%APPDATA%\SistemaDevolucoes\` (sobrevive a qualquer recompilação); em desenvolvimento continua na pasta do projeto.

Construída a tela de Catálogo (buscar produto por código de barras, cadastrar produto novo se não existir, adicionar/remover peça com foto e quantidade esperada) — testada de ponta a ponta, incluindo passada de responsividade mobile (grade de tamanho fixo em vez de elástica, card de peça em largura total abaixo de 480px, área de toque maior no "Remover", campos empilhados no formulário de adicionar peça em telas estreitas). Mudança de escopo importante: o usuário final (não só o dev) também vai usar essa tela de cadastro — ela deixa de ser "ferramenta interna descartável" e passa a exigir o mesmo cuidado de UX da tela de devolução. "Editar peça" ficou de fora por escolha (só adicionar/remover por ora).

**02/09/2026, 02:31** — Retomado o trabalho após a sessão anterior travar antes de atualizar o vault (nada foi perdido — conferido direto no repositório real, commit `8d593f3` batia com o checkpoint de 01:06). Primeiro, polimento na tela de Catálogo: preview de foto maior no formulário de adicionar peça, nome do arquivo fixo no rodapé do preview, e botão de escolher arquivo reestilizado (input nativo escondido + label como botão). Confirmado pelo usuário.

Na sequência, discutida e fechada uma reestruturação maior do fluxo de telas — detalhe completo em [[Reestruturação de Telas — Produtos como Tela Direta, Edição de Dados do Produto Embutida no Catálogo]] (nota de decisão nova, não duplicado aqui): gerado e aprovado um mockup interativo antes de qualquer código; implementação feita em 6 etapas pequenas e sequenciais, cada uma testada pelo usuário antes da próxima — campo `foto` no model `Produto` (+ migration); rotas e views novas (`produtos`, `editar_produto`); tela "Produtos" (listagem de produtos cadastrados); botão e formulário de cadastro de produto (com foto) movidos pra essa tela, view `cadastrar_produto` ajustada; seção "Dados do produto" (nome/marca/EAN/foto) dentro da tela de catálogo, com remoção do cadastro inline que existia na busca por código de barras; menu de navegação simples (Nova Devolução | Produtos) nas 3 telas. Ajuste fino de acabamento: nome do produto na grade da tela de Produtos agora quebra em 2 linhas em vez de cortar com reticências. Tudo testado e confirmado funcionando pelo usuário a cada etapa.

**02/09/2026, 02:45** — Definido o próximo passo: fazer a tela "Nova Devolução" funcionar de verdade e gerar o PDF completo com dados reais. Antes de codar, foram gerados 2 mockups interativos (aprovados pelo usuário) — um mostrando a marcação de peça (recebida/quantidade) alimentando um rascunho de PDF, outro focado só no layout do PDF, já cobrindo os 4 cenários reais de conferência (não recebida, parcial, completa com anotação, completa). Decisão tomada: a lógica de quantidade/déficit será implementada por completo já nessa rodada (não simplificada), mas o PDF nunca é salvo — é gerado e servido na hora, execução única, sem persistência em banco. Único ajuste pedido no rascunho: exibir também a foto de cada peça no PDF (as peças já têm imagem no catálogo). Checklist de implementação registrado em "Em aberto" abaixo, pra começar a implementação ponto a ponto.

**02/09/2026, 03:14** — Fechado o rascunho funcional da tela "Nova Devolução" + geração real do PDF de devolução, implementado ponto a ponto e testado a cada passo pelo usuário — detalhe completo em [[Geração do PDF de Devolução — xhtml2pdf, Sem Persistência e Fluxo por GET]] (nota de decisão nova, não duplicado aqui): busca real de produto/peças por código de barras, foto de produto e de peça exibidas na tela, lógica completa de quantidade/déficit (os 4 cenários), correção de um aviso de "confirmar reenvio" trocando o formulário pra GET, geração do PDF via `xhtml2pdf` com fotos incluídas (sem salvar nada no banco), e um ajuste visual final (fonte/espaçamento/fotos maiores, data em dd/mm/aaaa, removida a linha "Gerado em"). Usuário confirmou: "esta perfeito para um rascunho". Criado tutorial de como compilar e testar tanto em modo desenvolvimento quanto pelo `.exe` empacotado — ver [[Tutorial - Como Compilar e Testar o Sistema de Devolução (Com e Sem o .exe)]]; o teste com o `.exe` de verdade ainda não foi feito, fica pra amanhã.

**03/09/2026, 07:23** — Retomado o trabalho depois de uma sessão de testes do `.exe` empacotado que tinha ficado sem fechamento formal no vault (a sessão terminou sem tempo de atualizar antes de sair) — registrado agora, de forma retroativa, pra não perder o achado. 2 bugs reais de empacotamento foram encontrados nessa sessão, ambos exclusivos do `.exe` (nunca aparecem rodando via `runserver`):

1. **Import dinâmico do reportlab não detectado pelo PyInstaller**: ao abrir a tela de "Nova Devolução" dentro do `.exe`, o servidor caía com `ModuleNotFoundError: No module named 'reportlab.graphics.barcode.code128'`. Causa: o `xhtml2pdf` carrega, por baixo dos panos, um módulo do `reportlab` que monta suas classes de código de barras importando por string em tempo de execução (não um `import` escrito literalmente no código) — o PyInstaller só empacota o que consegue enxergar analisando o texto do código, então esse tipo de import passa despercebido. Correção proposta (ainda não testada): acrescentar a flag `--collect-submodules=reportlab.graphics.barcode` no comando de build do PyInstaller, forçando o empacotamento do subpacote inteiro.
2. **Banco de dados do `.exe` sem migração aplicada**: depois de corrigir o bug acima, apareceu um 2º erro, `django.db.utils.OperationalError: no such table: devolucoes_produto`, ao abrir a tela "Produtos". Causa confirmada direto no código real (`launcher.py`, clonado do GitHub só pra leitura, sem sincronizar/editar/executar nada): o caminho do banco em `%APPDATA%\SistemaDevolucoes\` já está correto (bug antigo do `BASE_DIR`, ver entrada de 02/09/2026 01:06, continua resolvido), mas o `launcher.py` nunca chama a migração do Django contra esse banco — então um banco novo (1ª execução, ou depois de recompilar) fica sem nenhuma tabela criada. Correção proposta (ainda não testada): chamar `call_command("migrate", verbosity=0)` dentro do `launcher.py`, antes de subir o servidor — e, como o carregamento de management commands do Django também usa import dinâmico (mesmo motivo do bug 1), acrescentar também a flag `--collect-submodules=django.core.management.commands` no build, de forma preventiva.

Nenhuma das 2 correções chegou a ser recompilada nem testada — faltou tempo antes da apresentação de hoje, e o computador disponível tem HD muito lento, o que torna cada ciclo de build do PyInstaller impraticável por ora. Por isso o `.exe` segue com status **desconhecido**, não **quebrado** — é diferente de um teste que já rodou e falhou.

Pra hoje, a apresentação do rascunho foi feita rodando via `runserver` (modo desenvolvimento, dentro do VS Code), acessada por outro dispositivo na mesma rede local (mesmo Wi-Fi/LAN, pelo IP da máquina + porta, ex: `10.0.0.123:8000`) — funcionou sem nenhum problema, os 3 fluxos principais (Produtos, Catálogo, Nova Devolução + PDF) foram demonstrados normalmente. Resultado: o rascunho foi **100% aprovado**. A partir de agora, o projeto deixa de ser só uma prova de conceito — vai ser desenvolvido como sistema real e robusto.

## Em aberto

- [x] Arquitetura de entrega do "app" — **fechada e decidida** (ver [[Arquitetura de Entrega do App de Devolução — PyInstaller Onedir, Loading HTML e Ícone de Bandeja]]). Sem pendência.
- [x] Schema exato do catálogo — **decidido e implementado**: Models Django (SQLite) `Produto` e `Peca`, peça sempre exclusiva de 1 produto. Banco e mídia persistem fora da pasta do app empacotado (`%APPDATA%`).
- [ ] Estrutura da interface — catálogo de peças, tela de Produtos (listagem + cadastro com foto) e edição de dados do produto (nome/marca/EAN/foto) construídos e testados; menu de navegação simples (Nova Devolução | Produtos) implementado. Faltam: editar peça já cadastrada (só adicionar/remover por ora), ligar a busca por código de barras da tela de devolução ao catálogo de verdade, e a interação de conferência de peça (marcar recebida/quantidade/anotação) ainda é só mockup, não código
- [x] Estrutura do relatório final de devolução gerado — implementado como **rascunho validado** (ver [[Geração do PDF de Devolução — xhtml2pdf, Sem Persistência e Fluxo por GET]] e linha do tempo 02/09/2026 03:14):
    - [x] Adicionar dependência `xhtml2pdf` ao projeto (Poetry)
    - [x] Ajustar view `nova_devolucao` pra buscar produto e peças reais do catálogo pelo código de barras
    - [x] Atualizar `nova_devolucao.html`: controle por peça conforme quantidade esperada, anotação liberada só quando recebida, foto de cada peça (e do produto)
    - [x] JS novo (`script_nova_devolucao.js`) com a lógica completa de status (não simplificada — os 4 cenários reais)
    - [x] View/lógica de geração do PDF (template dedicado + `xhtml2pdf`), incluindo as fotos, devolvida direto como resposta HTTP — sem salvar nada no banco
    - [x] Testado com produto real do catálogo, incluindo fotos — usuário validou como "perfeito para um rascunho"
    - [ ] Testar o `.exe` empacotado com as 2 correções propostas em 03/09/2026 (chamada de `migrate` automática no `launcher.py` + as 2 flags `--collect-submodules` no build do PyInstaller) — bloqueado por ora pelo HD lento do computador disponível, ver linha do tempo 03/09/2026 e [[Tutorial - Como Compilar e Testar o Sistema de Devolução (Com e Sem o .exe)]]
    - [ ] Ainda fora de escopo, por decisão do usuário: botão "Folha resumida", e qualquer persistência/histórico de devoluções

## Relacionado

- [[Processo de Devolução de Produtos e os 3 Caminhos Possíveis]]
- [[Agente Local Tinha 3 Bugs Reais no Empacotamento e Uma Limitacao no Pausar-Cancelar]]
- [[Reestruturação de Telas — Produtos como Tela Direta, Edição de Dados do Produto Embutida no Catálogo]]
