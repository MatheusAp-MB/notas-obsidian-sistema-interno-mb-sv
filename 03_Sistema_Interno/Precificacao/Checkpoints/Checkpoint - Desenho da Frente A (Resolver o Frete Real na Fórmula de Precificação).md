---
tipo: checkpoint
dominio: python
status: em_andamento
criado: 22/09/2026
atualizado_em: 23/09/2026 15:13
relacionado: [Checkpoint - Frete Real da API Implementado (Tela, Banco de Dados e Comando de Coleta em Massa), Descoberta - Endpoint de Frete Real do ML Confirmado em Anuncio Publicado, Simulacao Sem Item Nao Reproduz o Desconto Obrigatorio, Regras de Determinacao do Frete no Mercado Livre (Pesquisa Externa GPT), Bug Conhecido - Fallback do Produto ERP Sem Embalagem Fabricava Peso e Dimensao Zero, Gerando Sempre o Frete Mais Barato do ML]
---

# Checkpoint - Desenho da Frente A (Resolver o Frete Real na Fórmula de Precificação)

**Resumo**: Depois de fechar a Frente B (dado real populado no Passo 7 da auditoria — ver [[Checkpoint - Frete Real da API Implementado (Tela, Banco de Dados e Comando de Coleta em Massa)]]), a conversa virou pra Frente A: como ligar o frete real de verdade na fórmula de precificação (`resolver_preco_por_margem`, em `goal_seek.py`), resolvendo o risco já registrado na Seção 13 daquele checkpoint (frete_real depende do preço, mas é capturado só 1x por MLB, no preço publicado). Matheus pediu foco exclusivo nisso e foi construindo o raciocínio em etapas, validadas uma a uma nesta conversa. Nada disso foi implementado ainda — é desenho, ainda não virou diff nem código.

## 1. Princípio central — precificação é simulação, não relatório

Formulação de Matheus, key da discussão inteira: **"O preço do sistema não é o mesmo preço do Mercado Livre"**. O objetivo do sistema é único — criar o preço de venda a partir de custo + margem-alvo. Por isso, a pergunta que a Frente A precisa responder pra um MLB nunca é "qual é o frete real dele HOJE" (isso seria pergunta de relatório/auditoria — já é o que a Frente B faz no Passo 7) — é **"qual frete DEVERIA ter pra essas condições"** (peso/dimensão + preço que está sendo calculado agora). São perguntas diferentes, com fontes diferentes: relatório lê o anúncio publicado (`item_id`); simulação testa uma condição hipotética (`dimensions` + `item_price`, sem `item_id`).

## 2. Por que a referência de dimensão é o Produto do ERP, não o anúncio publicado

Em teoria, todos os anúncios de um mesmo produto deveriam ter a mesma dimensão (vêm do mesmo produto físico) — na prática, divergem por: erro de digitação no cadastro do anúncio, o ML remedindo o pacote no depósito e sobrescrevendo a dimensão declarada, ou instabilidade/erro do ML no cálculo. Por isso a referência do que "deveria ser correto" é sempre o **Produto cadastrado no ERP**, usando as dimensões de envio de lá — não a dimensão que cada anúncio individual declara.

**Escopo confirmado com Matheus (print da tela real)**: isso vale só pra **Grade Base (fallback do produto)** — a linha de `GradePrecificacaoML` com `variacao=None`, que já usa `origem_dimensao=produto_erp` hoje. Não muda nada nas linhas por variação (`origem_dimensao=variacao_ml`), que continuam preferindo a dimensão declarada no anúncio específico, como já é hoje. Não é uma mudança de comportamento — é o caminho que já existe, só que agora é ele que vai alimentar a simulação de frete real também.

## 3. Duas opções, em paralelo

- **Opção 1 — Tabela local (`FreteML`)**: já existe, já é o que `goal_seek.py`/`resolver_preco_por_margem()` usa hoje (ver mecanismo completo abaixo). Funciona pra qualquer faixa de preço, sem depender de API.
- **Opção 2 — API do ML em modo Simulação**: `GET /users/{user_id}/shipping_options/free` sem `item_id`, com `dimensions` (do ERP) + `item_price` (o preço sendo testado) + resto do contexto (`condition`/`category_id`/`listing_type_id`/`mode=me2`/`free_shipping="false"`). Receita validada em [[Descoberta - Endpoint de Frete Real do ML Confirmado em Anuncio Publicado, Simulacao Sem Item Nao Reproduz o Desconto Obrigatorio]] — **confirmada 44/44 (100%) em toda a faixa de preço** desde 22/09, 14:17 (o risco da seção 5, que só valia pra `item_price < R$79`, está resolvido — ver nota no fim da seção 5). **E agora confirmada também de ponta a ponta, encaixada no motor real de goal seek, com produto real: 4/4 margens idênticas à Opção 1** — ver seção 7 (22/09, 15:15).

Decisão de Matheus: rodar as 2 em paralelo (não substituir uma pela outra ainda) — **decisão que precisa ser revisitada agora que a limitação abaixo de R$79 não existe mais, e agora que a Opção 2 está provada de ponta a ponta** (ver Pendências).

## 4. Como o goal seek resolve isso hoje (mecanismo existente, reaproveitado)

`resolver_preco_por_margem()` (`precificacao/funcoes_auxiliares/goal_seek.py`) resolve a circularidade preço↔frete por **busca finita exata**, não por aproximação numérica:

1. Filtra as faixas de frete candidatas pelo peso do produto (`filtrar_faixas_frete()`) — geralmente poucas faixas sobrevivem.
2. Testa cada faixa candidata, pulando as com teto de preço menor que o custo do produto.
3. Pra cada faixa: assume o frete daquela faixa, calcula `preço_exato = (frete + fixo − rebate) ÷ denominador`, arredonda pra cima até ",90" (RoundUp90 — garante margem final ≥ meta).
4. Checa se o preço arredondado caiu dentro da própria faixa de preço que originou aquele frete (autoconsistência). Se sim, resolveu. Se não, descarta e tenta a próxima.
5. Resolve em 2 a 4 tentativas na prática (máximo 8 faixas no ML).

**O ponto que a Frente A precisa resolver**: esse mecanismo assume uma **lista de faixas candidatas com frete já conhecido de antemão** (a tabela local tem isso pronto). O frete real da API não tem isso — 1 chamada devolve 1 valor pra 1 preço específico. A Opção 2 dessa discussão é justamente sobre encaixar chamadas de API dentro desse mesmo loop de busca (chamar a API pra cada faixa candidata testada, em vez de só ler `faixa.valor` da tabela).

## 5. Exemplo trabalhado — e o risco crítico que ele expôs

Produto real usado como exemplo: **F7899947307029.001** (Chinelo Nuvem Sandália Ortopédica Fly Feet, branco), Tipo Clássico, custo R$73,43, 5 MLBs publicados.

- Dimensões de envio do ERP: 13×28×39cm, peso declarado 0,601kg
- Peso cubado: (13×28×39)÷6.000 = 2,366kg → maior que o físico, então peso faturável = **2,366kg**
- Faixa de peso 2,000–3,000kg tem 8 faixas de preço/frete candidatas na tabela local (de R$6,35 até R$27,05)
- Custo do produto (R$73,43) cai na faixa **R$49,00–78,99** — ponto de partida do goal seek, igual sempre foi

**Risco identificado nesta conversa**: testar a Opção 2 (API simulação) com `item_price` nessa faixa (R$49–78,99, abaixo de R$79) cai **exatamente** na zona que [[Descoberta - Endpoint de Frete Real do ML Confirmado em Anuncio Publicado, Simulacao Sem Item Nao Reproduz o Desconto Obrigatorio]] já mostrou não funcionar: nas 5 combinações testadas abaixo de R$79, `discount.type` veio `"fs_optional"` em vez de `"mandatory"`, o `list_cost` não bateu com a tabela real, e 3 preços diferentes abaixo de R$79 devolveram o mesmo `list_cost`/`promoted_amount` exatos — um "piso" ainda não diagnosticado. Ou seja: **pra esse exemplo específico (e pra qualquer produto cujas faixas candidatas caiam abaixo de R$79), a Opção 2 hoje não é confiável** — só a Opção 1 (tabela local) pode ser usada com confiança nessa faixa, até a investigação pausada (abaixo de R$79) ser retomada e resolvida.

**Resolvido em 22/09, 14:17**: a causa era o parâmetro `free_shipping="true"` — trocando pra `"false"`, a receita bateu 44/44 em toda a tabela, incluindo exemplos como o Chinelo acima. Esse risco não existe mais — ver [[Descoberta - Endpoint de Frete Real do ML Confirmado em Anuncio Publicado, Simulacao Sem Item Nao Reproduz o Desconto Obrigatorio]], seção "Confirmação Final (22/09, 14:17)".

## 6. Decisões fechadas nesta conversa

- **Fonte de dimensão pra Grade Base (fallback)**: sempre Produto do ERP — já é o comportamento atual (seção 2), sem mudança, não afeta linhas por variação.
- **Produto sem nenhum MLB publicado (em nenhum tipo)**: não usa a Opção 2 (API) de jeito nenhum — sem anúncio publicado não existe de onde puxar `category_id`. Usa só a Opção 1 (tabela local), do jeito que já funciona hoje.
- **Produto com ao menos 1 MLB publicado (qualquer tipo)**: `category_id` pode vir de **qualquer um** dos MLBs publicados daquele produto — categoria não muda entre Clássico/Premium do mesmo produto. `listing_type_id` **não precisa vir de nenhum MLB** — já é conhecido de antemão pela própria linha sendo calculada (`gold_special` quando a linha é Clássico, `gold_pro` quando é Premium — mapeamento confirmado em `mercado_livre/models/tipo_de_anuncio.py`). `condition` é sempre `"new"`, fixo.
- **`free_shipping`**: `true` ou `false`, tanto faz — já validado na receita original, sem efeito numérico dentro do mesmo regime de desconto.
- Confirmado (achado técnico, não decisão): `Produto` (ERP) só tem um campo `categoria` de texto livre — não existe `category_id` no formato do ML (`MLB1234`) cadastrado no ERP. Reforça por que o `category_id` só pode vir de um MLB real, nunca do cadastro do ERP direto.

## 7. Confirmação Final (22/09, 15:15) — Teste de Ponta a Ponta com Produto Real: 4/4 Validado

Depois da seção 5 confirmar a receita da API em toda a faixa de preço, Matheus pediu um teste isolado (fora do sistema) pra ver se a Opção 2 realmente se encaixa no motor de goal seek real, não só na chamada de API isolada. Script criado: `scripts_exploracao_ML/testar_goal_seek_via_api.py` — não reimplementa nenhuma conta, instancia a mesma classe de produção (`FormulaPrecificacao`, a mesma que `calcular_grade_precificacao_ml` usa) e o mesmo motor genérico (`resolver_preco_por_margem`, `goal_seek.py`), trocando só a origem do `frete_todas` (banco local vs. chamadas reais à API) — produto, dimensão, fixo, taxa e margem idênticos nas 2 opções, isolando só essa variável.

**Desenho da Opção 2 dentro do loop**: por faixa de preço candidata, 1ª chamada de API com o PISO da faixa (estimativa) → resolve com `resolver_preco_por_margem` (o motor real) → 2ª chamada (confirmação) com o `preco_90` calculado → se bater com a estimativa, fechado; se não bater, corrige só aquela faixa com o valor confirmado e refaz a busca.

**Produto testado**: Chinelo Nuvem Sandália Ortopédica Fly Feet (F7899947307029.001), custo R$73,43, peso faturável 2,366kg (cubado), Clássico, conta MB. 4 margens (Mínima 10%, Padrão 15%, Máxima 20%, Competição 5%).

**Resultado final: 4/4 IGUAL** — preço, frete e margem % obtida idênticos entre Opção 1 e Opção 2 nas 4 margens (Mínima R$123,90/frete R$21,65; Padrão R$132,90/frete R$21,65; Máxima R$142,90/frete R$21,65; Competição R$113,90/frete R$19,15), todas resolvidas em 1 rodada — a API confirmou exatamente o valor estimado no piso da faixa, sem precisar de correção.

**Dois achados colaterais no caminho, ambos corrigidos**:

- **Achado de dado (produção)**: a reimportação da planilha de frete (`Tabela_Frete_Mercado_Livre.xlsx`) tinha duplicado faixas de peso na tabela `FreteML` — em vez de atualizar os valores existentes, criou linhas novas com o mesmo peso e valores divergentes (ex: faixa `2,000–3,000` E `2,001–3,000` convivendo, com frete diferente pra mesma faixa de preço). Isso já tinha corrompido resultados da Opção 1 antes da correção (ex: Mínima calculava R$119,90/frete R$18,35 — valor fantasma de uma duplicata — em vez do correto R$123,90/frete R$21,65). Corrigido apagando a tabela (`FreteML.objects.all().delete()`, 464 registros removidos) e reimportando via `popular_banco` pras 2 empresas — 240 registros limpos (30 faixas de peso × 8 faixas de preço, sem duplicata), conferido visualmente na tela "Tabela de Frete — Mercado Livre" do sistema.
- **Achado de bug no script de teste (não é bug de produção)**: `taxa_percentual` precisa ser convertida de porcentagem pra fração (÷100) antes de entrar em `resolver_preco_por_margem` — o motor espera fração (documentado no próprio `goal_seek.py`), mas o campo público `DadosIntermediarios.taxa_percentual` (usado pra reaproveitar os mesmos números da Opção 1) guarda a forma de exibição (ex: `12.00`), não a fração (`0.12`) — essa conversão já existe internamente em `FormulaPrecificacao.montar_taxa_e_denominador()`, só não tinha sido replicada no script de teste. Sem isso, o denominador da fórmula ficava negativo e `resolver_preco_por_margem` retornava `None` de cara, antes de sequer olhar qualquer faixa de frete — por isso as 4 margens falhavam igual, com o mesmo motivo genérico, não relacionado a API nem a dado.

**Conclusão**: a abordagem "goal seek via API" (Opção 2) está tecnicamente provada de ponta a ponta, com produto real, encaixada no motor de produção de verdade — não é só a chamada de API isolada que funciona, é o fluxo completo (estimativa → goal seek → confirmação) que reproduz exatamente o resultado da Opção 1. A decisão de arquitetura (rodar as 2 em paralelo vs. só Opção 2) segue em aberto — ver Pendências.

## 8. Segundo Teste Revela o Gap do Teto de Metade do Preço — Confirmado com Doc Oficial do ML (23/09, 07:52)

Depois da seção 7 fechar 4/4 com o Chinelo (preços resolvidos todos acima de R$100), Matheus rejeitou a ideia inicial de alternar Clássico/Premium manualmente por constante no script e pediu o redesenho pra `--empresa`/`--ean` como parâmetros, calculando os 2 tipos automaticamente e mostrando os passos lado a lado — feito e validado (novo teste com o próprio Chinelo, 4/4 nos 2 tipos, 48/48 comparações passo a passo ✓). Na sequência, Matheus rodou o mesmo script num segundo produto real, propositalmente de custo baixo — e esse teste expôs um gap real que a seção 5 já tinha alertado como risco, mas ainda não confirmado.

**Produto testado**: EAN 7891988009757, custo R$1,99, peso faturável 0,809kg (cubado > físico), conta MB, Clássico (taxa 31,98%) e Premium (taxa 36,98%), 4 margens cada.

**Resultado: 0/4 IGUAL nas 2 tipos** (8 combinações). Diferente do Chinelo, aqui todos os preços resolvidos ficaram abaixo de R$19 (entre R$6,90 e R$17,90) — é exatamente essa faixa de preço que muda o comportamento.

**O padrão**: dentro da MESMA faixa oficial de preço (R$0,00–18,99), a Opção 2 (API) devolveu frete diferente por margem, sempre abaixo do valor fixo que a Opção 1 usa pro peso 0,5–1kg (R$6,05): R$1,95 (Competição, preço R$6,90), R$2,45 (Mínima, preço R$8,90), R$2,45 (Padrão, preço R$9,90), R$2,95 (Máxima, preço R$11,90) no Clássico — padrão parecido no Premium. Num caso (Premium/Máxima) a divergência foi além do valor: mudou de faixa — Opção 1 foi pra R$19,00–48,99 (frete R$7,15, preço final R$22,90), Opção 2 fechou dentro de R$0,00–18,99 (frete R$2,95, preço final R$12,90). É consequência direta do mesmo mecanismo, não um bug separado: com frete real mais baixo, a Opção 2 conseguiu fechar a conta sem precisar subir de faixa.

**Diagnóstico, lendo o código** (`buscar_list_cost_via_api`, em `testar_goal_seek_via_api.py`): a chamada é em `/users/{user_id}/shipping_options/free`, passando `item_price` como parâmetro — e o `list_cost` retornado é sensível ao preço testado, não só ao peso, como a tabela local `FreteML` assume (1 valor fixo por faixa peso×preço).

**Confirmado com a doc oficial do ML** ("Custos dos Envios no Mercado Livre para MercadoLíder, reputação verde ou sem reputação", trazida por Matheus): 2 achados —

1. A tabela-base bate exatamente com `FreteML`: peso 0,5–1kg / faixa R$0–18,99 = R$6,05 — confirma que a importação da planilha está correta, o problema não é dado errado.
2. Nota da própria doc, na coluna R$0–18,99: **"Os produtos de menos de R$19 pagam no máximo metade do preço do produto."** — este é exatamente o "novo gap" já registrado na Pendência de 22/09 (teto de frete pra `item_price < R$19` que `FreteML`/`goal_seek.py`/`calculo_margem.py` não implementam), até então uma suspeita não verificada. Agora está confirmado: fonte oficial do próprio Mercado Livre + evidência numérica batendo com o comportamento real da API.

**Ressalva**: os valores reais observados (R$1,95 a R$2,95) ficaram bem abaixo do teto de 50% do preço (que seria R$3,45 a R$5,95 nesses mesmos preços) — o "no máximo metade do preço" da doc é só o pior caso; existe uma fórmula mais fina por trás que a doc pública não revela. O teste em `item_price=R$0,01` (piso artificial da estimativa) retornando `list_cost=R$0,01` também se explica por isso — não era valor degenerado da API, era o teto agindo sobre um preço de teste extremo.

**Achado secundário, sem uso ainda**: a mesma doc lista uma tabela separada — "Custos por oferecer frete grátis rápido para produtos abaixo de R$79 (opcional)" (R$14,45 pro mesmo peso 0,5–1kg) — regime diferente (frete rápido opcional), não testado neste script (que usa `mode=me2`, `free_shipping=false`, regime padrão). Fica mapeado caso vire relevante depois.

**Conclusão**: o gap "teto de metade do preço" deixa de ser pendência teórica — está confirmado, com fonte oficial do ML e evidência numérica em produto real. Reforça diretamente a decisão de arquitetura em aberto (Opção 1 vs Opção 2 — seção 3/Pendências): a tabela local `FreteML` sistematicamente **superestima** o frete pra qualquer produto cujo preço final resolvido fique abaixo de R$19, o que pode estar inflando o preço calculado (e prejudicando competitividade) pros itens mais baratos do catálogo MB/SV.

## 9. Reputação da Conta e Regime de Envios Flex — Mapeados, Sem Impacto no Achado da Seção 8 (23/09, 07:59)

Matheus trouxe mais 2 documentos oficiais do ML pra completar o mapeamento das tarifas: "Costos de Venta" / "(864) Costos de Venta" (página-mãe de tarifas de venda) e "(864) Cargos por Envío" (detalhamento de frete + custos Full). Um terceiro doc reenviado ("Custos dos Envios no Mercado Livre para MercadoLíder, reputação verde ou sem reputação") é idêntico, linha por linha, ao já registrado na seção 8 — sem diferença nenhuma.

**Confirmação de comissão** (Costos de Venta): Clássico 10–14% conforme categoria, Premium 15–19% conforme categoria — bate com o que `FormulaPrecificacao.montar_taxa_e_denominador()` já usa como componente `comissao_percentual` do `taxa_percentual`. Sem achado novo aqui, só confirmação.

**Achado 1 — a tabela de custos varia por reputação da conta**: a página "Cargos por Envío" lista 3 tabelas de custos separadas, uma por faixa de reputação — "MercadoLíderes, com reputação verde ou sem reputação", "Reputação amarela", "Reputação laranja ou vermelha", cada uma com seu próprio "Consultar tabela de custos". O doc importado pro `FreteML` (seção 8) é especificamente a versão **verde/sem reputação**. **Confirmado por Matheus (23/09, 07:59): MB e SV estão garantidamente na faixa verde** — a tabela local está usando a versão de custos correta pras 2 contas. Risco verificado e descartado, sem necessidade de ação.

**Achado 2 — regime "Envios Flex" é separado do que o sistema usa hoje**: "Costos de Venta" e "Cargos por Envío" trazem uma tabela de custo fixo pro regime Envios Flex (logística própria do vendedor) pra produtos abaixo de R$79 — até R$12,50 → R$6,25; R$12,51–29 → R$6,50; R$29,01–50 → R$6,75 (mais uma tabela reduzida específica pra categoria "Livros"). Esse regime é diferente do que a Opção 2 testa (`mode=me2`, Coleta/Agências ML — regime padrão) e diferente do que `FreteML` guarda hoje. Fica mapeado, sem impacto na investigação atual — só citar se algum dia o modo Flex entrar em jogo.

**Resto da doc "Cargos por Envío"**: armazenagem diária Full, coleta a domicílio, retirada/descarte de estoque, inconformidade de envio (excesso/falta de unidades declaradas, cancelamento de reserva), estoque antigo, excesso de espaço — tudo sobre operação do Full, fora do escopo do `goal_seek`/preço por venda. Mapeado, sem ação necessária agora.

**Conclusão**: nenhum dos 2 achados desta rodada abre pendência nova. O risco de reputação foi verificado e fechado (MB/SV confirmadas verde); o regime Flex é só contexto adicional, não aplicável ao fluxo atual.

## 10. Produto Real com Frete "Por Conta do Comprador" Revela 2ª Tabela Não Importada — e Questiona a Pendência da Seção 8 (23/09, 08:19)

Matheus trouxe telas reais (Gestão de Anúncios, edição de anúncio e Simulador de custos, sem nenhum toggle manual) de um produto real com preço abaixo de R$19: Inseticida/Isca Mata Mosca Dipil, SKU F7898026081454.001, conta MB, 2 MLBs por tipo (Clássico R$16,86 — #6599423922 e #4597161489; Premium R$18,21 — #6599437466 e #4597084561).

**Peso confirmado**: embalagem de envio declarada 20×20×9cm, 0,4kg físico. Peso cubado = (20×20×9)÷6.000 = 0,6kg → maior que o físico → peso faturável 0,6kg, faixa "0,5 a 1kg" (a mesma faixa das Tabelas 1 e 2 já registradas na seção 8).

**Config atual real do anúncio: "Envio por conta do comprador"** (frete grátis NÃO está ativado). Confirmado por 2 fontes independentes, batendo com precisão de centavo: tela de Gestão de Anúncios ao vivo (Clássico R$16,86 → "Você recebe R$8,62"; Premium R$18,21 → "Você recebe R$8,88") e o Simulador de custos rodado sem tocar em nada (Clássico: tarifa R$2,19/13% + envio R$6,05 "Você não oferece frete grátis / Por conta do comprador" = recebe R$8,62; Premium: tarifa R$3,28/18% + envio R$6,05 = recebe R$8,88). Nos 2 casos o custo de envio efetivamente cobrado é **R$6,05** — exatamente a Tabela 1 (`FreteML`, peso 0,5–1kg / faixa R$0–18,99) já importada. Confirma, com produto e config reais, que a Tabela 1 está correta para esse cenário específico.

**O que resolveu a dúvida do print anterior (R$14,45 vs R$6,05)**: a tela de edição do anúncio mostra as 2 opções lado a lado, de onde o simulador tira o dado — "Oferecer frete por conta do comprador: você paga R$6,05" vs "Oferecer frete grátis: você paga ~~R$28,90~~ R$14,45 (cobrimos até 50% pela reputação)". O R$14,45 do print anterior era o simulador rodado com "oferecer frete grátis" marcado manualmente — não é o que está sendo cobrado hoje. R$14,45 bate exatamente com a Tabela 2 ("Custos por oferecer frete grátis rápido para produtos abaixo de R$79 — opcional", peso 0,5–1kg), já mapeada na seção 8 como achado secundário "sem uso ainda" — continua sem uso no sistema, mas agora com prova de que é uma tabela real, ativa, com valor bem diferente da Tabela 1 pro mesmo peso.

**Por que isso é mais importante que só resolver a dúvida do print**: `goal_seek.py`/`FreteML` foram desenhados pra precificar um produto assumindo que o frete entra no preço porque o vendedor VAI oferecer frete grátis (é a única razão do frete compor o `FIXO` da fórmula — se o comprador pagasse, não precisaria entrar na conta de formação de preço). Mas o anúncio real testado está configurado com frete por conta do comprador — e o próprio Mercado Livre marca esse anúncio como "Perdendo: outros vendedores oferecem condições melhores", recomendando "Ofereça frete grátis para que seu anúncio seja mais competitivo" (visível nas telas de gestão e de edição). Ou seja: a config real hoje (frete por conta do comprador) pode não refletir a intenção de competir que a fórmula assume.

Isso abre uma pergunta de negócio que só Matheus pode responder, e que muda qual tabela é a correta pra fórmula usar em produtos abaixo de R$79:

1. Se a estratégia real da MB/SV pra itens muito baratos é deixar por conta do comprador mesmo (como está esse anúncio) → Tabela 1 (R$6,05) está certa, e a pendência "teto de metade do preço" da seção 8 precisa ser reavaliada à luz disso — porque aquele teto de 50% era sobre o cenário de OFERECER frete grátis, que não é o que está sendo cobrado nesse anúncio hoje.
2. Se a intenção é competir com frete grátis (literalmente o que o ML está recomendando nesse anúncio) → a fórmula deveria estar usando a Tabela 2 (R$14,45 nesse peso), que **não existe em nenhuma tabela do banco hoje** — um gap diferente do "teto de metade do preço", e maior: não é um teto sobre um valor existente, é uma tabela inteira ausente.

**Ressalva que continua de pé**: nem a Tabela 1 (R$6,05) nem a Tabela 2 (R$14,45) batem com os valores que `testar_goal_seek_via_api.py` retornou pro outro produto testado (EAN 7891988009757, ~R$1,95–2,95 pra faixa equivalente) — esse fio da investigação (seção 8) segue não explicado por este achado; é um problema separado, ainda em aberto.

**Conclusão**: achado de alto valor, ainda sem decisão — não é bug nem correção pronta, é uma decisão de arquitetura/negócio (qual regime de frete a fórmula deve assumir pra produtos <R$79) que precisa ser tomada antes de qualquer implementação nessa frente.

## 11. Confirmação Controlada via API — `free_shipping` Realmente Alterna Entre as 2 Tabelas, e o Teto de Metade do Preço Não Se Aplica ao Valor Com Frete Grátis (23/09, 09:24)

Pra fechar a dúvida metodológica da seção anterior (será que a API, chamada pelo sistema, bate com o que as telas reais mostram?), Matheus pediu um teste controlado, isolado da comparação de margem do goal seek. Script novo: `scripts_exploracao_ML/testar_matriz_frete_gratis_via_api.py` — chama `buscar_list_cost_via_api` direto (sem passar por `resolver_preco_por_margem`), nos 2 preços reais do Inseticida da seção 10 (Clássico R$16,86, Premium R$18,21, lidos do banco via `VariacaoAnuncioMercadoLivre.preco_atual`, não fixados no script), com a dimensão declarada no ML (20×20×9cm, 0,4kg físico — peso cru, deixando a própria API calcular o cúbico), variando `free_shipping` (false/true) × tipo de anúncio (Clássico/Premium) — 4 combinações, comparadas contra o gabarito das telas reais da seção 10 (R$6,05 sem frete grátis, R$14,45 com).

**Resultado: 4/4 bateram, no centavo** — Clássico e Premium deram exatamente R$6,05 com `free_shipping=false` e exatamente R$14,45 com `free_shipping=true`, sem nenhuma diferença entre os 2 tipos de anúncio.

**3 coisas confirmadas de uma vez**:
1. `free_shipping` é mesmo o parâmetro que decide entre Tabela 1 e Tabela 2 — não é coincidência nem artefato de toggle manual na tela, é comportamento real e determinístico da API.
2. Nenhuma das 2 tabelas varia por tipo de anúncio (Clássico/Premium) — só por peso, confirmando a leitura da doc oficial das seções 8/9.
3. A própria API calculou o peso cúbico certo a partir da dimensão física crua (20×20×9cm/0,4kg) — não foi preciso pré-calcular o peso faturável (0,6kg) do lado do sistema pra bater.

**Achado que mexe direto na pendência do teto de metade do preço (seção 8)**: nenhum dos 2 preços testados (R$16,86 e R$18,21, ambos abaixo de R$19) resultou em `list_cost` capado em metade do preço — metade de R$16,86 seria R$8,43, metade de R$18,21 seria R$9,105, e o valor retornado com frete grátis foi R$14,45 nos 2 casos, bem acima desses tetos. Ou seja: o "no máximo metade do preço" da doc oficial **também não se aplicou no cenário de frete grátis** (Tabela 2) — não é só a Tabela 1 (já visto na seção 10) que escapa do teto. Continua não sabido a que regime/faixa exata esse teto se refere — só ficou mais claro, por eliminação, que não é nem a Tabela 1 nem a Tabela 2 nos preços testados aqui.

**Ainda não resolvido**: os valores baixos (R$1,95–2,95) do produto da seção 8 (EAN 7891988009757) continuam sem explicação — nem a Tabela 1 nem a Tabela 2 batem com aquele resultado, e este teste (produto diferente, mesma faixa de peso e preço) não reproduziu esse comportamento. Pode ser algo específico daquele produto/categoria, ainda não isolado.

**Conclusão**: o mecanismo `free_shipping` está agora provado de forma controlada e determinística (4/4, 2 tipos de anúncio, 2 fontes de gabarito independentes — doc oficial e tela real). Isso destrava tecnicamente a decisão de negócio da seção 10 (qual tabela a fórmula deve assumir pra produtos abaixo de R$79) — falta só a decisão de Matheus sobre qual regime reflete a estratégia real da empresa pra esses produtos.

## 12. Bateria com 50 Candidatos Reais — 6 Divergências, 1 Confirmada por Tela Real como Dado Local Desatualizado (Não Bug de API), e Descoberto Desconto de 50% por Reputação no Frete Grátis (23/09, 10:17)

Depois do mecanismo `free_shipping` provado de forma controlada (seção 11, 1 produto só), Matheus pediu uma bateria maior: buscar candidatos reais no catálogo cobrindo o máximo de faixas de peso/preço distintas da tabela real, priorizando bordas de faixa, e já testar cada um via API na mesma rodada. Script novo: `scripts_exploracao_ML/buscar_e_testar_candidatos_diversos_frete_via_api.py` — busca em `VariacaoAnuncioMercadoLivre` (dado declarado no ML, mesmo motivo da seção 11: é o que a API reflete de verdade, não o ERP), calcula peso faturável localmente (mesma fórmula: cúbico = AxLxC÷6.000, faturável = máximo entre cúbico e físico), classifica cada candidato na tabela real de 30 faixas de peso × 8 faixas de preço (a mesma da seção 8, agora copiada integralmente no script), prioriza quem está perto de uma borda de faixa (peso, preço, ou dos limiares R$19/R$79), e testa cada selecionado contra `buscar_list_cost_via_api`, comparando `free_shipping=false` (Tabela 1) contra o valor calculado da tabela real (com o teto de metade do preço aplicado pra `item_price < R$19` — seção 8).

**Resultado da 1ª rodada (conta MB, 50 candidatos)**: 44/50 bateram exatamente. 6 divergências, em 2 padrões bem distintos:

- **Padrão 1 (5 casos)**: o `list_cost` obtido bate exatamente com a célula da tabela real correspondente à faixa de peso IMEDIATAMENTE ABAIXO da que o script calculou localmente — mesma faixa de preço, faixa de peso errada por 1 posição. Casos: MLB4589791003, MLB6290498506 (ambos "0,5-1kg" esperado, bateu com "0,3-0,5kg"); MLB3524129241 ("1-1,5kg" esperado, bateu com "0,5-1kg"); MLB5762455114 ("2-3kg" esperado, bateu com "1,5-2kg"); MLB6844156208 ("15-17kg" esperado, bateu com "13-15kg").
- **Padrão 2 (1 caso)**: MLB4035124163, preço R$599,00 (bem dentro da faixa "a partir de R$200", sem ambiguidade de borda) — obtido bateu com a célula da faixa de preço "R$150–199,99", não "a partir de R$200". Ainda não investigado com tela real (fica pendência).

**Investigação do MLB4589791003 (Padrão 1) com tela real do Simulador de custos — muda o diagnóstico**: Matheus trouxe 2 prints da tela real desse MLB (sem e com "oferecer frete grátis" marcado, sem nenhum outro toggle). Achados, com precisão de centavo:

1. **Preço real na tela: R$78,99** — o banco tinha `preco_atual=R$78,90`. Mesma faixa de preço (49–78,99) nos 2 casos, então não muda o resultado aqui, mas é uma 2ª confirmação (a 1ª foi o caso do MLB4035124163, ainda hipótese) de que o dado importado pode ficar desatualizado em relação ao que está publicado agora.
2. **Sem frete grátis, tela mostra custo de envio R$8,25** ("Por conta do comprador") — bate exatamente com o `list_cost` que a API retornou pro nosso script (o "obtido"). Ou seja: **a API está certa — o "esperado" R$8,45 do nosso script é que estava errado**, porque R$8,25 corresponde à faixa "0,3 a 0,5kg", não "0,5 a 1kg" como o script assumiu a partir do dado declarado gravado no banco (`altura_declarada_cm`/`largura_declarada_cm`/`comprimento_declarado_cm`/`peso_declarado_kg`).
3. **Conclusão do padrão**: não é diferença de arredondamento entre o cálculo de peso cúbico da API e o nosso (hipótese inicial, descartada) — é o dado declarado local (dimensão/peso) desatualizado em relação ao que está publicado no ML agora. Confirmado com prova de tela pra este 1 caso; mesma assinatura exata nos outros 4 casos do Padrão 1 (obtido bate com a célula da faixa imediatamente abaixo) é forte indício de que a mesma causa se aplica — mas isso segue **hipótese, não confirmado individualmente** pros outros 4.

**Achado novo, não mapeado em nenhuma seção anterior — desconto de 50% "por reputação" no frete grátis**: a tela com "oferecer frete grátis" marcado mostrou custo de envio nominal **R$27,70, riscado**, com o valor efetivo **R$13,85** ao lado e a nota "Nós cobrimos o 50% pela sua reputação!" — R$27,70 ÷ 2 = R$13,85, exato. Isso nunca tinha aparecido nas seções 8–11 (que só registraram 1 valor fixo de Tabela 2, R$14,45, pro Inseticida Dipil). Como esse Inseticida também usa o mesmo campo `preco_atual`/dimensão declarada como fonte, e a Tabela 2 não foi testada com esse nível de detalhe lá, **fica em aberto se o R$14,45 já confirmado nas seções 10/11 é nominal (sem desconto) ou já vem com esse desconto de reputação aplicado** — não dá pra saber sem reabrir aquele teste capturando o campo `discount` da resposta.

**Ajuste feito no script por causa desses 2 achados**: `buscar_list_cost_via_api` foi renomeada pra `buscar_simulacao_frete_via_api` e agora devolve, além do `list_cost`, o `billable_weight` (peso faturável que a PRÓPRIA API calculou) e o `discount` de cada chamada — gravados lado a lado com o peso faturável calculado localmente (`peso_faturavel_kg_nosso` x `peso_faturavel_kg_api`) na tabela de resultado e no JSON de saída. Isso deixa qualquer divergência futura da bateria autoexplicada (mostra se é peso desatualizado, e mostra o desconto de reputação aplicado), sem precisar de script separado nem de print de tela pra cada caso. **Diff entregue, ainda não aplicado nem rodado até este registro** — pendência.

**Conclusão**: a bateria em diversidade de produtos fez o que devia — achou pontos de quebra reais. Mas a causa raiz, pelo menos pro caso investigado a fundo, não é a API nem o cálculo do sistema — é desatualização do dado local declarado (dimensão/peso e/ou preço) importado no banco. Isso é um achado de INTEGRIDADE DE DADO, não de lógica de frete — muda o tipo de correção necessária (rotina de reimportação/atualização mais frequente do dado declarado, não ajuste de fórmula) e levanta uma dúvida metodológica sobre até que ponto os "gabaritos" usados nas seções 10/11 (que vieram do mesmo tipo de campo) continuam válidos hoje.

## 13. Causa Raiz das Divergências da Seção 12 Identificada e Corrigida — Bug de Fronteira de Faixa de Peso, Não Dado Desatualizado (23/09, 10:48)

Depois do diff pendente da seção 12 (captura de `billable_weight`/`discount`) ser aplicado e a bateria rodada de novo, os 6 casos divergentes vieram com o peso faturável que a PRÓPRIA API calculou (`billable_weight`) — permitindo comparar direto contra o peso faturável calculado localmente, sem precisar de mais nenhuma tela real.

**Resultado da comparação, caso a caso**: nos 6 casos (os 2 já detalhados do antigo "Padrão 1", mais MLB3524129241, MLB5762455114, MLB4035124163 — o antigo "Padrão 2" — e MLB6844156208), o `billable_weight` da API bateu EXATO com o peso faturável calculado localmente (`peso_faturavel_kg_nosso == peso_faturavel_kg_api`, sem exceção nos 6). **Isso derruba a hipótese de dado local desatualizado levantada na seção 12** — o peso que o banco guarda (declarado no ML) está certo, e a própria API confirma calculando o mesmo número.

**A causa real**: em todos os 6 casos, o peso faturável cai EXATAMENTE em cima do limite de uma faixa da tabela real (500g, 1.000g, 2.000g, 6.000g, 15.000g) — e o `list_cost` que a API cobrou bate, célula por célula (mesma faixa de preço), com a faixa de peso IMEDIATAMENTE ABAIXO da que `encontrar_faixa_peso()` (a função nova do script da seção 12) atribuía. A função comparava com `peso_min <= peso < peso_max` (fronteira fechada embaixo, aberta em cima) — a evidência mostra que a convenção real do Mercado Livre é o oposto: **fronteira fechada em cima, aberta embaixo** (`peso_min < peso <= peso_max`). Ou seja: 500g ainda conta como "até 0,5kg", não como "a partir de 0,5kg".

Isso também resolve o antigo "Padrão 2" da seção 12 (MLB4035124163) — não era uma divergência de faixa de PREÇO como parecia (o preço R$599 estava solidamente dentro de "a partir de R$200", sem ambiguidade nenhuma de borda) — era o mesmo bug de peso: 6.000g caindo errado em "De 6 a 7 kg" (deveria cair em "De 5 a 6 kg"). As 6 divergências da bateria eram uma causa única, não duas padrões distintos.

**Fix aplicado em `buscar_e_testar_candidatos_diversos_frete_via_api.py`**: `encontrar_faixa_peso()` reescrita pra checar as faixas em ordem crescente e retornar a primeira cujo `peso_max` seja `>=` o peso faturável (fronteira fechada em cima). Não precisou de "gap" artificial entre faixas nem de converter peso pra grama inteira — funciona direto em Decimal, porque a comparação vira um `<=` só, sem precisar achar um buraco entre 2 valores.

**Validação (rodada após o fix, conta MB)**: **50/50 bateram, 0 divergências.** Cobertura: 22/30 faixas de peso, 6/8 faixas de preço — não é cobertura total da tabela ainda, pode ser ampliada numa bateria futura se for útil.

**Achado colateral — 2 tipos de `discount` observados no campo `sem_fg_discount`, sem relação com o bug de fronteira**:
- Nos 4 casos "normais" (peso/preço mais baixos): `{"rate": 0.3, "type": "none", "promoted_amount": 0}` — presente em toda chamada, mas parece inerte (`type: "none"` e `promoted_amount: 0` sugerem metadado sempre presente na resposta, não um desconto de fato aplicado; não foi preciso considerá-lo pra bater os 50/50).
- Em 2 casos (MLB4035124163, R$599,00/6kg Premium; MLB6844156208, R$855,34/15kg Clássico): `{"rate": 0.5, "type": "mandatory", "promoted_amount": <2x o list_cost>}` — e nesses 2 casos, `sem_fg_obtido` e `com_fg_obtido` vieram IDÊNTICOS, sugerindo que o frete grátis é OBRIGATÓRIO pra esses anúncios (o parâmetro `free_shipping=false` não muda o resultado). Ainda não mapeado o que determina isso (categoria? threshold de preço? threshold de peso? Full?) — mesmo assim, o valor final bateu certinho com a Tabela 1 corrigida, então não bloqueou a validação.

**O que isso NÃO resolve**: o teto de metade do preço (seções 8/11) e os valores baixos não-explicados do produto EAN 7891988009757 (seção 8) continuam sem relação com este achado — são investigações separadas, ainda em aberto.

**Conclusão**: o diagnóstico da seção 12 (dado local desatualizado) está corrigido — o problema era 100% lógico, no código do script de exploração, não nos dados. Isso acende um alerta importante: se a mesma lógica de fronteira existir na fórmula de precificação REAL (não só no script de teste), o mesmo tipo de erro pode estar subestimando frete — e margem — de produtos reais cujo peso faturável cai exatamente num limite de faixa. Ainda não verificado — pendência nova abaixo.

## 14. Tabela 1 e Tabela 2 Oficiais Consolidadas em Planilha de Referência — Ciclo de Edição Manual Revisado e Validado (23/09, 13:31)

Com o doc oficial completo do ML ("Custos dos Envios no Mercado Livre para MercadoLíder, reputação verde ou sem reputação", já citado nas seções 8/9) em mãos, Matheus pediu a Tabela 1 e a Tabela 2 (esta última só tinha 1 célula conhecida até aqui — R$14,45 pro peso 0,5–1kg, seção 10) extraídas por completo e consolidadas numa planilha de referência.

**Extração confirmada**: a Tabela 1 (30 faixas de peso × 8 faixas de preço) bate célula por célula com o que já está importado em `FreteML` — não é achado novo, é confirmação. A Tabela 2 (30 faixas de peso × 1 faixa de preço, R$0–78,99) foi extraída por completo pela primeira vez, direto do HTML oficial — nenhum valor calculado ou estimado.

**Planilha montada**: `Tabela_Custos_Envios_Mercado_Livre.xlsx`, salva em `Claude outputs/`. Desenho pensado pra servir tanto de consulta humana quanto de leitura por código: 1 tabela única (não 2 abas separadas), 1 par de linhas por faixa de peso ("Sem Frete Grátis Rápido" / "Com Frete Grátis Rápido"), coluna explícita de `Regime` e do parâmetro `free_shipping` (`true`/`false`) que a API espera, código de cores diferenciando as 2 linhas, e comentário em cada célula das colunas ≥R$79 nas linhas "Com Frete Grátis Rápido" explicando que o valor ali é espelhado da Tabela 1 — porque a partir de R$79 o frete grátis rápido já é padrão embutido na própria Tabela 1, não existe uma 2ª tabela separada pra essa faixa (nota oficial do doc, seção 8).

**1ª rodada de edição manual de Matheus — mistura de melhoria deliberada e efeito colateral acidental**: analisada célula a célula (valor, preenchimento, fonte, borda, alinhamento, altura de linha, largura de coluna, comentário). Melhorias deliberadas: cabeçalho recolorido em 3 grupos batendo com os regimes de preço (R$0–18,99 neutro / R$19–78,99 laranja / R$79+ amarelo); legenda e título compactados em mesclagens mais estreitas. Efeitos colaterais acidentais, sem relação com nenhuma edição deliberada: as 30 linhas "Com Frete Grátis Rápido" perderam o preenchimento azul por completo (0 exceção, todas as colunas); negrito foi parar em 844 das 962 células (era 226), incluindo células vazias — assinatura de "selecionar tudo → negrito"; altura de linha uniformizada em 33pt sem necessidade nas linhas de dado. Corrigido programaticamente (restaurando só fonte/preenchimento/altura das áreas afetadas a partir do arquivo original, preservando 100% das mesclagens/cores de cabeçalho/larguras que Matheus tinha ajustado). Confirmado 0 divergência de valor, fórmula (não há) e comentário em toda a checagem — só formatação foi tocada.

**2ª rodada de edição manual — a validada agora como padrão final**: fonte padronizada pra Arial 12pt em toda a planilha (antes variava 9/10/11/13pt conforme a área); as 4 notas de rodapé (fonte/peso/reputação/teto de metade do preço) ganharam negrito além do itálico; moldura adicionada ao redor do bloco de notas de rodapé; larguras de coluna e alturas de linha ajustadas de forma coerente com a fonte maior. Analisada da mesma forma — **0 divergência de valor, 0 de preenchimento, 0 de alinhamento, 0 de formato numérico**, e as únicas mudanças de fonte além de tamanho/família foram o negrito das 4 notas, aplicado de forma uniforme, sem nenhuma célula fora do padrão. Diferente da 1ª rodada, esta não teve nenhum efeito colateral acidental — validada como está, sem necessidade de correção.

**O que esta planilha É e o que NÃO é**: é a referência definitiva e validada das 2 tabelas oficiais de frete do ML (com e sem frete grátis rápido), pronta pra consulta humana e pra leitura programática futura (colunas `Regime`/`free_shipping` explícitas, sem ambiguidade). **Não é** uma implementação — não altera `FreteML`, `goal_seek.py` nem nenhum código de produção, e não resolve nenhuma das pendências técnicas abertas abaixo (bug de fronteira em produção, teto de metade do preço, qual regime a fórmula deve assumir, gatilho do desconto `mandatory`). É material de apoio pra quando essas decisões forem tomadas.

**Conclusão**: as 2 tabelas oficiais de frete do ML estão, pela primeira vez, completamente extraídas, conferidas e num formato único que documenta a regra (não só os números) — incluindo o "por quê" de cada valor via comentários de célula. O ciclo de revisão manual + verificação célula a célula (2 rodadas) funcionou como rede de segurança: pegou os 2 efeitos colaterais acidentais da 1ª rodada antes de virarem padrão, e confirmou que a 2ª rodada é limpa.

## 15. Bug de Fronteira em Produção — Primeira Resposta Empírica: Ambiguidade Existe, mas Não Causou Nenhuma Divergência em 50 Candidatos Reais (23/09, 14:19)

A seção 13 fechou o bug de fronteira dentro do script de exploração, mas deixou em aberto a pergunta maior, registrada nas Pendências e feita a Matheus 2x sem resposta ainda: **a fórmula de precificação real (`goal_seek.py`/`filtrar_faixas_frete()`) usa a mesma convenção de fronteira?** Se usasse a mesma convenção corrigida na seção 13 (fechada só em cima), não teria problema — mas a leitura do código de produção mostrou o contrário: `filtrar_faixas_frete()` (em `precificacao/funcoes_auxiliares/mercado_livre/formula_precificacao.py`, e as mesmas condições replicadas em `formula_precificacao_tiktok.py`, `formula_precificacao_magalu.py`, `montar_linhas_precificacao.py` e `gerar_relatorio_frete_erp_vs_ml.py`) usa **fronteira fechada nos 2 lados** (`peso_min <= peso <= peso_max`) — convenção diferente da que a seção 13 confirmou como empiricamente correta, e que gera ambiguidade real: num peso que cai exatamente numa fronteira compartilhada (500g, 1.000g, 2.000g, 6.000g, 15.000g...), a consulta de produção bate com 2 linhas de `FreteML` ao mesmo tempo, não 1.

**Como a ambiguidade se resolve em produção, na teoria**: `FreteML.Meta.ordering = ['peso_min', 'preco_min']` ordena a tabela sempre pelo menor peso primeiro, e `resolver_preco_por_margem()` (`goal_seek.py`) usa uma busca que retorna o primeiro candidato autoconsistente que encontra — ou seja, no empate de 2 linhas, produção sempre pega a de menor `peso_min`. Isso levanta uma dúvida imediata: essa faixa de menor peso é a correta (a que a seção 13 confirmou empiricamente, fronteira fechada só em cima) ou é a errada (a faixa "de baixo", que deveria valer só até o peso anterior)? Na primeira leitura desta conversa, a análise concluiu — errado — que produção pegaria a faixa errada. Numa reanálise, ficou claro que é o oposto: a faixa de menor `peso_min` que ordena primeiro é estruturalmente a MESMA que a regra empírica da seção 13 considera correta pra aquele valor exato de fronteira. Ou seja, a ambiguidade parece se resolver sozinha, corretamente, por coincidência da ordenação — não por design.

**Teste empírico pra confirmar, não só analisar**: `buscar_e_testar_candidatos_diversos_frete_via_api.py` (o mesmo script da seção 13) foi estendido — sem tocar em nenhuma lógica já validada — pra também calcular o frete pela lógica REAL de produção, lado a lado com "Esperado (tabela)" e "Obtido (API)", fechando a comparação de 3 pontas que Matheus propôs. Mudanças: import de `FreteML`; carga única de `FRETE_ML_TODAS = list(FreteML.objects.all())` (respeitando o `Meta.ordering` real); nova função `calcular_frete_producao_local(peso_faturavel_kg, item_price)`, que reproduz `filtrar_faixas_frete()` exatamente (mesma condição fechada nos 2 lados, mesmo critério "primeiro candidato que bate"), devolvendo o valor e a quantidade de candidatos que bateram (pra detectar ambiguidade); campos novos no resultado (`sem_fg_calculado_producao`, `sem_fg_producao_qtd_candidatos`, `sem_fg_producao_bate`, `producao_ambigua`); coluna nova na tabela de saída e bloco de resumo agregado (`Esperado (tabela) x Calculado (produção, sem API): X/Y bateram`). Diff entregue em Localize/Substitua, aplicado por Matheus, sincronizado e conferido linha a linha depois (2 rodadas de sync — a 1ª pegou 11 das 12 mudanças aplicadas, faltando só o bloco de resumo agregado; a 2ª, corrigida e reentregue, confirmou as 12/12).

**Resultado da bateria (conta MB, 50 candidatos, mesma seleção que já validava 50/50 desde a seção 13)**:

- **Esperado (tabela) x Calculado (produção, sem API): 50/50 bateram — 0 divergências.**
- **17/50 candidatos sinalizados como ambíguos** (peso faturável caindo exatamente numa fronteira compartilhada). As 17 ocorrências se concentram em 8 valores de fronteira distintos: 0,3 / 0,5 / 1 / 1,5 / 2 / 5 / 8 / 13 kg.
- Em **todos os 17 casos ambíguos**, sem exceção, o valor que "produção usaria" bateu exatamente com o valor confirmado correto pela tabela — nenhum dos 17 caiu na faixa errada.

**O que isso confirma, com dado real e não só leitura de código**: a hipótese da coincidência de ordenação se sustenta — nos 50 candidatos testados (17 deles pisando exatamente numa fronteira ambígua), a ambiguidade estrutural de `filtrar_faixas_frete()` nunca gerou um resultado de frete errado. A causa é a mesma explicada acima: a linha que `FreteML.Meta.ordering` coloca primeiro no empate é sempre a de menor `peso_min`, e essa é também a linha que a convenção real do ML (confirmada na seção 13) considera correta pra aquele peso exato.

**O que isso NÃO fecha — pendência residual, mais estreita que a original**: este teste reproduz `filtrar_faixas_frete()` isoladamente, no formato de consulta direta (1 peso, 1 preço → 1 resultado). `resolver_preco_por_margem()`, o motor real usado em produção, faz uma busca mais ampla, testando várias faixas de PREÇO candidatas em sequência (não só a faixa de peso exata) até achar a autoconsistente — um cenário onde peso E preço caem em fronteiras ambíguas ao mesmo tempo não foi exercitado por este teste. A pergunta original ("produção tem o mesmo bug?") tem agora uma primeira resposta empírica — sim, tem a ambiguidade estrutural, mas ela não gerou erro nos casos testados — porém a pergunta mais estreita (o comportamento se mantém com ambiguidade dupla, peso+preço, dentro do goal seek completo?) continua em aberto.

**Conclusão**: o risco levantado na seção 13 (fronteira em produção subestimando frete/margem) tem agora evidência real, não só leitura de código, de que — pelo menos no formato de lookup direto testado — é inofensivo. A ambiguidade existe e é estrutural (não foi corrigida em produção, nem deveria ser só por causa deste achado, já que o efeito observado é nulo), mas o motivo dela não quebrar nada em 50 casos reais está agora explicado, não é sorte cega: é a mesma ordenação (`peso_min`, `preco_min`) que decide o empate sendo, por coincidência, a mesma regra que decide qual faixa é a correta.

## 16. Tabela 2 (Com Frete Grátis Rápido) Validada Empiricamente contra a API Real — 50/50, Mesma Bateria da Seção 15 (23/09, 14:34)

Com a Tabela 1 já validada de ponta a ponta (seção 13/15), Matheus pediu o mesmo tratamento pra Tabela 2 — até aqui só extraída do doc oficial (seção 14) e confirmada em 1 única célula isolada, com 1 produto (seção 11). O script `buscar_e_testar_candidatos_diversos_frete_via_api.py` foi estendido de novo (mesma bateria de 50 candidatos da seção 15, sem trocar seleção nem tocar em nada já validado): `TABELA_FRETE` ganhou o campo `valor_com_fg` em cada uma das 30 faixas de peso (valor único, extraído célula a célula do `Tabela_Custos_Envios_Mercado_Livre.xlsx` da seção 14 — confirmado que as 3 primeiras colunas de preço, R$0–18,99/R$19–48,99/R$49–78,99, sempre têm o mesmo valor pra Com FG, e que a partir de R$79 o valor espelha exatamente a Tabela 1, sem exceção nas 30 linhas); nova função `calcular_gabarito_tabela_2()` (sem nenhum teto de metade do preço — a seção 11 já tinha confirmado, com teste controlado 4/4, que esse teto não se aplica ao regime de frete grátis); campo novo `com_fg_esperado` e propriedade `com_fg_bate` no resultado, comparando contra o `com_fg_obtido` que o script já vinha coletando via API (`free_shipping=true`) desde a seção 12, mas sem gabarito pra comparar até agora. Diff entregue em Localize/Substitua (11 mudanças), aplicado por Matheus, sincronizado e conferido linha a linha depois — 11/11 confirmadas, sem nenhuma pendência de aplicação desta vez.

**Resultado da bateria (conta MB, mesmos 50 candidatos da seção 15)**: **Esperado (Tabela 2) x Obtido (API, com FG): 50/50 bateram — 0 divergências.** Fecha os 2 regimes na mesma rodada:

- Esperado (tabela) x Obtido (API) — Sem FG: 50/50
- Esperado (tabela) x Calculado (produção, sem API) — Sem FG: 50/50 (17 ambíguos por fronteira compartilhada, seção 15, todos resolvidos certo)
- Esperado (Tabela 2) x Obtido (API) — Com FG: 50/50

**Por que isso importa mais do que "mais um teste passando"**: é a primeira vez que a Tabela 2 é testada em escala (30 faixas de peso, não só 1 célula) contra a API real — até aqui só tinha a confirmação isolada da seção 11. Os 2 candidatos que mostraram `discount.type == "mandatory"` no teste Sem FG (MLB4035124163, MLB6844156208, seção 13) também bateram 0 divergência aqui — consistente, já que ambos têm preço acima de R$79, faixa em que a Tabela 2 espelha a Tabela 1 por regra oficial (não é coincidência do teste, é a própria regra se confirmando de novo).

**O que isso NÃO fecha totalmente — pendência do desconto de 50% por reputação (seção 12) reduzida, não eliminada**: nenhum dos 50 candidatos testados (incluindo os com preço abaixo de R$79, onde a Tabela 2 tem valor próprio e um desconto por reputação teria efeito visível) mostrou divergência que apontasse pra esse desconto de 50% visto na tela real da seção 12 (R$27,70 riscado → R$13,85 efetivo). Isso é evidência de que o desconto não se aplicou em nenhum dos 50 casos — não descarta que ele exista (foi visto ao vivo numa tela real), só que nenhum dos 50 candidatos testados reproduziu a condição que o dispara. O gatilho exato (categoria? histórico de vendas? algo específico daquele anúncio da seção 12?) segue sem mapeamento.

**Conclusão**: a Tabela 2 agora tem o mesmo nível de confiança empírica que a Tabela 1 — extraída do doc oficial E confirmada 50/50 contra chamadas reais à API, cobrindo as 30 faixas de peso. A decisão de arquitetura da seção 10/Pendências (qual regime a fórmula deve assumir pra produtos abaixo de R$79 — Tabela 1 "por conta do comprador", já implementada, ou Tabela 2 "frete grátis rápido", ainda ausente do banco) está tecnicamente pronta pra qualquer lado que Matheus decidir — os dados dos 2 regimes estão validados; falta só a decisão de negócio, não mais nenhuma validação técnica.

## 17. O "Desconto de 50% por Reputação" Explicado por Completo — É Tabela 1 Tiered por Reputação, Não um Desconto de Frete Grátis (23/09, 15:03)

A descoberta da seção anterior (R$90,50 riscado → R$45,25 efetivo, no Simulador de custos do MLB3519337227) ficou totalmente explicada quando Matheus trouxe as 3 tabelas oficiais completas do doc do ML, uma por faixa de reputação — "laranja ou vermelha", "amarela" e "MercadoLíderes, com reputação verde ou sem reputação" (a mesma estrutura de 3 tabelas já mapeada na seção 9, mas agora com os valores completos das 3, não só a verde).

**Conferência célula a célula, linha "De 6 a 7 kg", nas 5 colunas de preço ≥ R$79**:

| Faixa de preço | Verde (a nossa) | Amarela | Laranja/Vermelha |
|---|---|---|---|
| R$79–99,99 | 27,55 | 33,06 | 55,10 |
| R$100–119,99 | 31,65 | 37,98 | 63,30 |
| R$120–149,99 | 36,75 | 44,10 | 73,50 |
| R$150–199,99 | 40,85 | 49,02 | 81,70 |
| A partir de R$200 | 45,25 | 54,30 | **90,50** |

Exato, sem nenhuma variação de centavo nas 5 colunas: **Amarela = 1,20× Verde**, **Laranja/Vermelha = 2,00× Verde**. O R$90,50 "riscado" que apareceu na tela da seção anterior é literalmente o valor da própria Tabela 1 (Sem FG) da faixa de reputação laranja/vermelha, pro mesmo peso e preço — não é um valor de "frete grátis nominal" inventado.

**Reinterpretação correta**: não existe um desconto especial de frete grátis sendo aplicado. O Mercado Livre tem uma Tabela 1 inteira, tiered por reputação (já sabido desde a seção 9) — cada faixa de reputação paga um valor diferente pelo MESMO frete, com ou sem frete grátis, nas faixas de preço ≥ R$79. O Simulador de custos usa o pior tier (laranja/vermelha) como referência visual "riscada" pra ilustrar a economia de ter reputação boa — mas a conta verde (MB/SV) paga o valor verde de qualquer forma, é a tabela normal dela, sem cálculo de desconto nenhum por cima. Isso também explica retroativamente o achado da seção 13: o `promoted_amount` = 2× `list_cost` nos casos `discount.type == "mandatory"` é exatamente esse valor de referência laranja/vermelha, vindo cru da API.

**Confirmação explícita de Matheus (23/09, 15:03)**: a referência oficial usada em todo o sistema — `FreteML`, `TABELA_FRETE` dos scripts de exploração, e a Tabela 1/2 consolidada da seção 14 — é e continua sendo especificamente a tabela **"Custos dos Envios no Mercado Livre para MercadoLíder, reputação verde ou sem reputação"** (o mesmo doc já citado desde a seção 8). Isso fica registrado aqui de forma explícita, não só implícita: qualquer manutenção futura nessa área deve manter essa mesma fonte, salvo decisão em contrário.

**Sobre o risco de mudança de reputação (nomeado na seção 16/mensagem anterior)**: Matheus confirmou que a reputação da conta não deve mudar, e que se mudar, isso será percebido e as tabelas serão trocadas manualmente pra faixa correta — não é um risco a mitigar preventivamente no código agora. Fica registrado como premissa conhecida e aceita, não como pendência aberta: o sistema assume reputação verde/sem reputação; se isso mudar, a `TABELA_FRETE` (scripts de exploração) e o `FreteML` (produção) precisariam ser reimportados com os valores do tier correto — os fatores de conversão (1,20× pra amarela, 2,00× pra laranja/vermelha, confirmados só pra essa 1 faixa de peso/preço ≥R$79 até aqui) dão um atalho pra estimar o impacto, mas a reimportação completa é o caminho correto quando/se isso acontecer.

**Conclusão**: fecha, com evidência dupla (doc oficial completo + tela real), a origem do "desconto de 50%" observado nas seções 12 e 16 — não é um mecanismo separado de frete grátis, é a Tabela 1 tiered por reputação se manifestando através da UI do Simulador. Nenhuma mudança de código é necessária — a Tabela 1/2 já usada em todo o sistema já é, e continua sendo por decisão explícita, a tabela certa pra reputação verde/sem reputação.

## 18. Pesquisa Técnica Encerrada, Implementação em Pausa — Aguardando Decisão de Negócio/Arquitetura (23/09, 15:13)

Matheus encerrou a fase de investigação da Frente A nesta conversa pra abrir outra investigação em paralelo. Registro de onde isso fica, pra retomar depois sem perder o fio.

**O que está genuinamente fechado, sem pendência técnica**: peso faturável e a convenção de fronteira de faixa (seção 13); se produção tem o mesmo bug de fronteira — testado com dado real, ambíguo mas inofensivo em 50/50 casos no formato de lookup direto (seção 15); Tabela 1 e Tabela 2 extraídas do doc oficial e validadas em escala contra a API real, 50/50 nos 2 regimes (seções 14/15/16); o "desconto de 50% por reputação" explicado por completo — é a Tabela 1 tiered por reputação, não um mecanismo separado (seção 17); que a Opção 2 (API ao vivo) se encaixa tecnicamente no motor real de goal seek, provada de ponta a ponta com produto real (seção 7).

**O que falta não é mais pesquisa, é decisão de Matheus** — nenhuma delas foi tomada ainda:

1. Qual regime usar pra produtos abaixo de R$79 — deixar por conta do comprador (Tabela 1, já é o que produção faz hoje) ou competir com frete grátis (Tabela 2, ausente do banco)? Estratégia comercial da MB/SV, não pergunta técnica.
2. Opção 1 (tabela local) continua só, Opção 2 (API ao vivo) substitui, ou as 2 ficam em paralelo? Se a resposta for "só Opção 1", praticamente nada precisa mudar — produção já está correta pro que já faz.
3. Se entrar Opção 2: simulação ao vivo (dentro do cálculo de cada linha) ou em lote separado (job, tipo o `buscar_frete_real_ml` de hoje)? Muda a arquitetura inteira da implementação.

**Pontas soltas menores, não bloqueiam as 3 decisões acima nem a implementação**: teto de metade do preço com 1 caso ainda sem explicação completa (EAN 7891988009757, seção 8/11); gatilho exato de `discount.type` (`mandatory` vs `fs_optional` vs `none`) não mapeado; arredondamento de peso cúbico fracionário em fronteira não testado; ambiguidade dupla peso+preço no `resolver_preco_por_margem()` completo não exercitada (residual da seção 15).

**Estado do código**: nenhum diff foi aplicado em produção durante toda essa investigação — só nos scripts de exploração (`scripts_exploracao_ML/`), sempre entregues em Localize/Substitua e aplicados manualmente por Matheus. Produção continua rodando exatamente como antes de a Frente A começar.

**Retomada**: quando Matheus voltar pra esta frente, o ponto de entrada é a seção "Pendências / próximos passos" abaixo — as 3 decisões estão marcadas lá. Não precisa reler o checkpoint inteiro; as 18 seções documentam o raciocínio caso alguma decisão precise ser revisitada, mas a ação seguinte está resumida nesta seção 18 e na lista de pendências.

## Pendências / próximos passos

- **Como a Opção 2 se encaixa no loop do goal seek, tecnicamente — RESOLVIDA em 22/09, 15:15**: desenhada, implementada num script de teste isolado e validada 4/4 com produto real. Ver seção 7.
- **Pergunta operacional em aberto**: a simulação via API roda ao vivo, dentro do cálculo de cada linha de precificação (potencialmente muitas chamadas por produto × tipo × margem, toda vez que a grade é recalculada), ou em lote separado, parecido com o `buscar_frete_real_ml` de hoje (que roda como job isolado, não durante o cálculo)? Não decidido ainda.
- **Limitação abaixo de R$79 da Opção 2 — RESOLVIDA em 22/09, 14:17**: causa raiz era `free_shipping="true"` — corrigido pra `"false"`, revalidação completa (44/44, as 3 partes originais) confirmou 100%, sem exceção. Detalhe completo: [[Descoberta - Endpoint de Frete Real do ML Confirmado em Anuncio Publicado, Simulacao Sem Item Nao Reproduz o Desconto Obrigatorio]], seção "Confirmação Final (22/09, 14:17)". **Decisão aberta**: isso muda "Opção 1 + Opção 2 em paralelo" pra "só Opção 2 cobrindo a faixa inteira"? ou as 2 continuam em paralelo por outro motivo (ex: robustez, fallback se a API cair)?
- **Gap do teto de metade do preço (`item_price < R$19`) — CONFIRMADO em 23/09, 07:52, EM REAVALIAÇÃO desde 23/09, 08:19, AINDA MAIS RESTRITO desde 23/09, 09:24**: era suspeita da revalidação de 22/09; confirmado com produto real (EAN 7891988009757, custo R$1,99) e fonte oficial do ML ("produtos de menos de R$19 pagam no máximo metade do preço do produto"). Ver seção 8. A seção 10 mostrou que esse teto não se aplica à Tabela 1 (frete por conta do comprador); a seção 11 mostrou, com teste controlado 4/4, que também não se aplica à Tabela 2 (frete grátis) nos preços testados (R$16,86/R$18,21). Ou seja, o teto não foi observado em nenhum dos 2 regimes já mapeados — continua sem explicação o que causou os valores baixos (R$1,95–2,95) do produto da seção 8, que segue como o único caso real onde esse comportamento apareceu.
- **Qual regime de frete a fórmula deve assumir pra produtos abaixo de R$79 — aberta em 23/09, 08:19, mecanismo confirmado tecnicamente em 23/09, 09:24, AMBOS OS REGIMES VALIDADOS EM ESCALA em 23/09, 14:34**: achado da seção 10. Existem 2 tabelas oficiais bem diferentes pro mesmo peso (ex: 0,5–1kg → R$6,05 "sempre aplicado, por conta do comprador" vs R$14,45 "oferecer frete grátis, opcional abaixo de R$79") e o sistema só tem a primeira importada. A seção 11 confirmou, de forma controlada (4/4), que a API alterna corretamente entre as 2 tabelas via `free_shipping`; a seção 16 estendeu essa confirmação pra escala real — 50/50 candidatos reais, cobrindo as 30 faixas de peso, Tabela 2 batendo exato com a API. A parte técnica (as 2 tabelas são confiáveis, nos 2 regimes) está agora tão resolvida quanto pode estar sem implementação. Falta só a decisão de negócio: qual delas a fórmula deveria usar depende da estratégia real da MB/SV pra itens baratos, deixar por conta do comprador (Tabela 1, já implementada) ou competir com frete grátis (Tabela 2, ausente do banco hoje — precisa de campo de regime no `FreteML` e reescrita do importador, ver seção 14). **Decisão de Matheus, não técnica.**
- **Confirmar Padrão 1 nos outros 4 casos — RESOLVIDA em 23/09, 10:48, com causa diferente da hipótese**: não era dado local desatualizado — era bug de fronteira em `encontrar_faixa_peso()` (fechava a faixa errada). Corrigido e validado 50/50. Ver seção 13.
- **Investigar Padrão 2 — RESOLVIDA em 23/09, 10:48, mesma causa do Padrão 1**: MLB4035124163 não era divergência de faixa de preço — era o mesmo bug de fronteira de peso (6.000g caindo em "De 6 a 7 kg" em vez de "De 5 a 6 kg"). Ver seção 13.
- **Desconto de 50% por reputação no frete grátis — EXPLICADO POR COMPLETO em 23/09, 15:03**: não é um desconto separado de frete grátis. A seção 17 confirmou, com o doc oficial completo (3 tabelas por reputação) mais a tela real, que o "riscado" visto nas seções 12/16 é literalmente a própria Tabela 1 (Sem FG) do tier laranja/vermelha (2,00× a verde) ou amarelo (1,20× a verde) — a tabela verde que já usamos em todo o sistema é, ela mesma, o valor final pra conta MB/SV, sem nenhum cálculo de desconto por cima. O campo `discount`/`promoted_amount` capturado desde a seção 12 (`rate`, `type: mandatory/fs_optional/none`) segue sem 100% de mapeamento de quando cada tipo aparece, mas isso não bloqueia mais nada — o valor final (`list_cost`) já vem certo da API pra reputação verde, confirmado 50/50 nas seções 15 e 16.
- **Risco metodológico — dado declarado local pode estar desatualizado — REAVALIADO em 23/09, 10:48, não era a causa das 6 divergências da bateria**: a seção 13 confirmou, via `billable_weight` da própria API, que o peso local batia exato nos 6 casos — a causa era 100% lógica (fronteira de faixa), não dado desatualizado. A observação isolada de preço (R$78,90 no banco vs R$78,99 real, MLB4589791003, seção 12) continua de pé como achado menor, mas não impactou nenhum resultado. Risco rebaixado de prioridade, não descartado — vale reconferir se aparecer em teste futuro.
- **Verificar se a fórmula de precificação real (produção) tem o mesmo bug de fronteira de faixa de peso — PRIMEIRA RESPOSTA EMPÍRICA em 23/09, 14:19, RESIDUAL MAIS ESTREITO SEGUE ABERTO**: `filtrar_faixas_frete()` (produção) usa fronteira fechada nos 2 lados — diferente da convenção corrigida na seção 13 — e gera ambiguidade real em pesos exatos de fronteira (2 linhas de `FreteML` batendo ao mesmo tempo). Bateria de 50 candidatos reais, comparando "Esperado (tabela)" contra o cálculo local exato de `filtrar_faixas_frete()` (reproduzido fielmente com dado real de `FreteML`, não mockado), deu **50/50 bateram, 0 divergências** — incluindo os 17/50 casos sinalizados como ambíguos, todos resolvidos pro valor correto. Explicação: `FreteML.Meta.ordering = ['peso_min', 'preco_min']` faz o empate ser sempre resolvido pela linha de menor `peso_min`, que coincide com a linha que a convenção real do ML considera correta pra aquela fronteira — não por design, mas por coincidência de ordenação. Ver seção 15. **O que segue em aberto**: este teste reproduz só `filtrar_faixas_frete()` isolada; `resolver_preco_por_margem()` testa várias faixas de PREÇO em sequência, e um cenário de ambiguidade dupla (peso E preço na fronteira ao mesmo tempo) não foi exercitado.
- **Mapear a regra do frete grátis "obrigatório" (`discount.type == "mandatory"`) — NOVA em 23/09, 10:48**: achado colateral da seção 13, 2 dos 6 candidatos testados (MLB4035124163, MLB6844156208 — ambos pesados/caros) vieram com `sem_fg_obtido == com_fg_obtido`, sugerindo que `free_shipping=false` é ignorado nesses casos. Ainda não sabido o gatilho (peso? preço? categoria? Full?).
- **Confirmar arredondamento do peso cúbico fracionário quando domina o físico e não é grama redonda — NOVA em 23/09, 10:48**: todos os candidatos com `peso_cubico_domina` testados até agora (seção 13) deram peso cúbico em grama redonda exata (1.000kg, 2.000kg) — nenhum caso real testou um peso cúbico fracionário (tipo os 0,3966...kg que aparecem nos cálculos, mas nesses casos o físico sempre dominava). Direção de arredondamento da API nesse cenário (pra cima/pra baixo/mais próximo) segue não confirmada — não bloqueia o fix da seção 13, mas fica pendência se aparecer candidato assim numa bateria futura.
- **Planilha de referência das Tabelas 1 e 2 oficiais — FINALIZADA em 23/09, 13:31**: `Tabela_Custos_Envios_Mercado_Livre.xlsx`, em `Claude outputs/`, com as 2 tabelas completas, código de cores e colunas `Regime`/`free_shipping` prontas pra leitura por código. Ver seção 14. É só material de referência — não resolve nenhuma pendência técnica abaixo.
- **Ambiguidade dupla peso+preço no `resolver_preco_por_margem()` completo — NOVA em 23/09, 14:19**: residual mais estreito deixado pela seção 15. A bateria validou `filtrar_faixas_frete()` isolada (50/50, 0 divergências mesmo nos 17 casos ambíguos), mas não o motor completo de goal seek testando múltiplas faixas de preço em sequência com peso também ambíguo. Não bloqueia nada hoje (o achado é que o efeito observado é nulo) — só não está 100% descartado como cenário de borda.
- **Tabela 2 (Com Frete Grátis Rápido) validada em escala contra a API — RESOLVIDA em 23/09, 14:34**: 50/50 candidatos reais, cobrindo as 30 faixas de peso, 0 divergências. Ver seção 16. Junto com a Tabela 1 (seção 13/15), os 2 regimes de frete agora têm confiança empírica equivalente — o que faltava pra decisão de arquitetura da seção 10 ser só decisão de negócio, sem nenhuma dúvida técnica pendente sobre os dados em si.
- **Referência de reputação usada no sistema — EXPLICITADA em 23/09, 15:03**: confirmado por Matheus que `FreteML`/`TABELA_FRETE`/Tabela 1 e 2 consolidadas usam, por decisão explícita (não só herança implícita do doc importado), a tabela "MercadoLíder, reputação verde ou sem reputação". Reputação da conta não deve mudar; se mudar, será percebida e as tabelas trocadas manualmente pro tier correto (fatores de conversão 1,20×/2,00× mapeados pra 1 faixa na seção 17, como atalho de estimativa). Ver seção 17. Não é mais pendência — é premissa registrada.
- **Investigação em pausa — encerrada em 23/09, 15:13**: Matheus fechou a fase de pesquisa desta frente pra abrir outra investigação em paralelo. Ver seção 18 pro resumo do que está fechado e das 3 decisões de negócio/arquitetura que faltam antes de qualquer implementação. Retomar por aqui quando voltar.
- Matheus ainda vai continuar o raciocínio — este checkpoint reflete só o que foi validado até 23/09, 15:13. Nenhum diff foi aplicado no sistema de produção ainda — só nos scripts de exploração.

## Relacionado

- [[Checkpoint - Frete Real da API Implementado (Tela, Banco de Dados e Comando de Coleta em Massa)]]
- [[Descoberta - Endpoint de Frete Real do ML Confirmado em Anuncio Publicado, Simulacao Sem Item Nao Reproduz o Desconto Obrigatorio]]
- [[Regras de Determinacao do Frete no Mercado Livre (Pesquisa Externa GPT)]]
- [[Bug Conhecido - Fallback do Produto ERP Sem Embalagem Fabricava Peso e Dimensao Zero, Gerando Sempre o Frete Mais Barato do ML]]
